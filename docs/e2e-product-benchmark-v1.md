# Product E2E Benchmark v1

Scenario: Sales + Inventory + Purchasing source files.

Required flow:
Import → Schema Discovery → Validation → Reconciliation → Canonicalization → Evidence → Semantic Metrics → Risk Detection → Explanation → Recommendation → Alternatives → Approval → Department Tasks → Execution → Outcome → Executive Report → PDF.

Acceptance dimensions:
- Correct
- Tenant-safe
- Deterministic
- Evidence-bound
- Actionable
- Auditable
- Reproducible

Evidence bundle:
source fixture hashes, import job IDs, reconciliation IDs, metric versions, rule versions, evidence IDs, recommendation/decision IDs, task IDs, outcome IDs, report ID/version, PDF checksum.

Runtime status:
DESIGN/READY until staging is available. This file does not claim E2E PASS.
