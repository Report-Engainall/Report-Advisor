# Assertions

The repair must preserve both tenant-bound FK paths and provide index coverage for each nullable reference. The contract gate asserts exactly two index definitions in the dedicated migration.
