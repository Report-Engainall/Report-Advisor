# Import truth closure

The import compatibility boundary now preserves authoritative lifecycle counters and cannot fabricate validation results from processed rows. This change is intentionally kept isolated until full CI verifies it.