# Provenance Index Repair Addendum

The live Staging repair was rechecked after migration application. Performance Advisor no longer reports the two unindexed composite foreign keys on `canonical_text_provenance`. This repository branch preserves that live schema state for replay parity.
