# Verification

Expected live objects:

- canonical_text_provenance_company_file_idx
- canonical_text_provenance_company_folder_idx

Expected predicates:

- file_id is not null
- folder_id is not null

The repository contract test verifies these exact migration tokens.
