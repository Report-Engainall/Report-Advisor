import { chromium } from 'playwright';
import assert from 'node:assert/strict';
import fs from 'node:fs/promises';
import crypto from 'node:crypto';
import XLSX from 'xlsx';
import { scenarios } from './production-scenario-matrix.mjs';
import { snapshotBusinessState, assertZeroUnintendedMutation, assertTenantPeriodCurrency, assertRenderedJob, assertEvidenceProvenance, assertNoFalseCommit, assertDuplicateIdempotency, scenarioAssertionPlan, buildCompactEvidence, validateCompactArtifact } from './production-scenario-contract.mjs';
const baseURL=(process.env.E2E_BASE_URL||'http://127.0.0.1:4173').replace(/\/$/,'');
const supabaseURL=(process.env.REPORT_ADVISOR_SUPABASE_URL||'').replace(/\/$/,'');
const anonKey=process.env.REPORT_ADVISOR_SUPABASE_ANON_KEY?.trim(); const email=process.env.TEST_USER_A_EMAIL?.trim(); const password=process.env.TEST_USER_A_PASSWORD; const exactHead=process.env.EXACT_HEAD||'UNKNOWN'; const out='release-evidence/production-regression-results.json';
for(const [n,v] of Object.entries({supabaseURL,anonKey,email,password})) if(!v) throw new Error(`SCENARIO_E2E_ENV_MISSING:${n}`); await fs.mkdir('release-evidence',{recursive:true});
function csv(rows){const h=Object.keys(rows[0]);return Buffer.from(`\ufeff${h.join(',')}\n${rows.map(r=>h.map(k=>JSON.stringify(String(r[k]??''))).join(',')).join('\n')}\n`)}
function xlsx(rows){const wb=XLSX.utils.book_new();XLSX.utils.book_append_sheet(wb,XLSX.utils.json_to_sheet(rows),'Sheet1');return XLSX.write(wb,{type:'buffer',bookType:'xlsx'})}
function pdf(text){const esc=String(text).replaceAll('\\','\\\\').replaceAll('(','\\(').replaceAll(')','\\)');const stream=`BT /F1 12 Tf 50 760 Td (${esc}) Tj ET`;const objects=['<< /Type /Catalog /Pages 2 0 R >>','<< /Type /Pages /Kids [3 0 R] /Count 1 >>','<< /Type /Page /Parent 2 0 R /MediaBox [0 0 612 792] /Resources << /Font << /F1 5 0 R >> >> /Contents 4 0 R >>',`<< /Length ${Buffer.byteLength(stream)} >>\nstream\n${stream}\nendstream`,'<< /Type /Font /Subtype /Type1 /BaseFont /Helvetica >>'];let out='%PDF-1.4\n';const offsets=[0];for(let i=0;i<objects.length;i++){offsets.push(Buffer.byteLength(out));out+=`${i+1} 0 obj\n${objects[i]}\nendobj\n`;}const xref=Buffer.byteLength(out);out+=`xref\n0 ${objects.length+1}\n0000000000 65535 f \n`;for(let i=1;i<=objects.length;i++)out+=`${String(offsets[i]).padStart(10,'0')} 00000 n \n`;out+=`trailer\n<< /Size ${objects.length+1} /Root 1 0 R >>\nstartxref\n${xref}\n%%EOF\n`;return Buffer.from(out,'binary')}
function imagePdf(){return pdf('Arabic OCR اختبار ١٢٣٫٤٥ confidence review DQS')} function corrupt(){return crypto.randomBytes(256)}
const suffix=`${Date.now()}-${process.pid}`; const customer=n=>({name:`Scenario Customer ${suffix}-${n}`,code:`SC-${suffix}-${n}`,phone:'777000000',email:`scenario-${suffix}-${n}@example.invalid`}); const product=n=>({sku:`SC-SKU-${suffix}-${n}`,name:`Scenario Product ${suffix}-${n}`,cost_price:10,selling_price:15,unit:'piece'}); const invoice=n=>({invoice_number:`SC-INV-${suffix}-${n}`,invoice_date:new Date().toISOString().slice(0,10),customer_name:`Scenario Customer ${suffix}-${n}`,total:15,currency:'SAR'});
const inputs=new Map([['excel-standard',{entity:'products',name:`excel-standard-${suffix}.xlsx`,bytes:xlsx([product(1)])}],['excel-aliases',{entity:'customers',name:`excel-aliases-${suffix}.xlsx`,bytes:xlsx([{'Customer Name':customer(2).name,Code:customer(2).code,Phone:customer(2).phone}])}],['excel-missing-columns',{entity:'products',name:`excel-missing-columns-${suffix}.xlsx`,bytes:xlsx([{sku:`SC-MISSING-${suffix}`,name:'Missing price'}])}],['csv-reordered',{entity:'customers',name:`csv-reordered-${suffix}.csv`,bytes:csv([{email:customer(4).email,code:customer(4).code,phone:customer(4).phone,name:customer(4).name}])}],['pdf-text',{entity:'sales_invoices',name:`pdf-text-${suffix}.pdf`,bytes:pdf('invoice_number SC-PDF-001 invoice_date 2026-09-15 customer_name Scenario PDF total 15 currency SAR')}],['pdf-ocr-ar',{entity:'sales_invoices',name:`pdf-ocr-ar-${suffix}.pdf`,bytes:imagePdf()}],['unknown-report',{entity:'sales_invoices',name:`unknown-report-${suffix}.pdf`,bytes:pdf('Quarterly narrative memorandum without a canonical financial schema')}],['exchange-statement',{entity:'sales_invoices',name:`exchange-statement-${suffix}.pdf`,bytes:pdf('Debit 100 Credit 40 Opening Balance 60 Closing Balance 60 Currency SAR')}],['multi-currency',{entity:'sales_invoices',name:`multi-currency-${suffix}.csv`,bytes:csv([{...invoice(9),currency:'EUR'}])}],['duplicate-transactions',{entity:'customers',name:`duplicate-transactions-${suffix}.csv`,bytes:csv([customer(10)])}],['large-file',{entity:'customers',name:`large-file-${suffix}.csv`,bytes:csv(Array.from({length:1000},(_,i)=>customer(`large-${i}`)))}],['corrupt-data',{entity:'sales_invoices',name:`corrupt-data-${suffix}.pdf`,bytes:corrupt()}]]);
const browser=await chromium.launch({headless:true}); const context=await browser.newContext({viewport:{width:1440,height:1000},locale:'ar-SA'}); const page=await context.newPage(); let accessToken=null,tenantId=null; const results=[];
async function login(){await page.goto(baseURL,{waitUntil:'networkidle',timeout:30000});await page.locator('#login-email').fill(email);await page.locator('#login-password').fill(password);await page.getByRole('button',{name:'تسجيل الدخول'}).click();await page.waitForURL(u=>!u.pathname.includes('/login'),{timeout:30000});accessToken=await page.evaluate(()=>{const raw=Object.entries(localStorage).find(([k])=>k.endsWith('-auth-token'))?.[1];if(!raw)throw new Error('BROWSER_SESSION_NOT_FOUND');return JSON.parse(raw).access_token});const r=await fetch(`${supabaseURL}/rest/v1/rpc/current_company_id`,{method:'POST',headers:{apikey:anonKey,Authorization:`Bearer ${accessToken}`,'Content-Type':'application/json'},body:'{}'});const b=await r.text();assert.equal(r.ok,true,`tenant HTTP ${r.status}: ${b}`);tenantId=b.replaceAll('"','').trim();assert.match(tenantId,/^[0-9a-f-]{36}$/i)}
async function rest(table,select,filters={}){const u=new URL(`${supabaseURL}/rest/v1/${table}`);u.searchParams.set('select',select);for(const[k,v]of Object.entries(filters))u.searchParams.set(k,`eq.${v}`);const r=await fetch(u,{headers:{apikey:anonKey,Authorization:`Bearer ${accessToken}`}});const b=await r.text();assert.equal(r.ok,true,`${table} HTTP ${r.status}: ${b}`);return b?JSON.parse(b):[]}
async function rpc(name,body={}){const r=await fetch(`${supabaseURL}/rest/v1/rpc/${name}`,{method:'POST',headers:{apikey:anonKey,Authorization:`Bearer ${accessToken}`,'Content-Type':'application/json'},body:JSON.stringify(body)});const b=await r.text();assert.equal(r.ok,true,`${name} HTTP ${r.status}: ${b}`);return b?JSON.parse(b):null}
async function snapshot(){const [ij,inv,mov,ev,dash]=await Promise.all([rest('import_jobs','id,status,source_hash,company_id',{company_id:tenantId}),rest('sales_invoices','id,invoice_number,company_id,currency,total,amount',{company_id:tenantId}),rest('inventory_movements','id,product_id,movement_type,company_id,quantity,total',{company_id:tenantId}),rest('kpi_evidence_snapshots','id,metric_key,company_id,as_of',{company_id:tenantId}),rpc('get_dashboard_snapshot',{})]);return snapshotBusinessState({importJobs:ij,invoices:inv,movements:mov,evidence:ev,dashboard:dash})}
async function runOne(scenario,input){const started=new Date().toISOString(),fp=crypto.createHash('sha256').update(input.bytes).digest('hex');const r={scenario_id:scenario.id,exact_head:exactHead,tenant:tenantId,input_fingerprint:fp,job_id:null,execution_start:started,execution_end:null,actual_status:'NOT_PROVEN',terminal_state:null,expected_status:scenario.expect,assertions:[],business_assertions:[],negative_assertions:[],before_state:null,after_state:null,dashboard:null,persistence_readback:null,evidence_references:[],failure:null};try{r.before_state=await snapshot();await page.goto(`${baseURL}/import`,{waitUntil:'networkidle',timeout:30000});const label=input.entity==='customers'?'العملاء':input.entity==='products'?'المنتجات':'فواتير المبيعات';await page.getByRole('button',{name:new RegExp(label)}).click();await page.locator('input[type="file"]').first().setInputFiles({name:input.name,mimeType:input.name.endsWith('.pdf')?'application/pdf':input.name.endsWith('.xlsx')?'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet':'text/csv',buffer:input.bytes});const preview=page.getByText('المراجعة',{exact:true});try{await preview.waitFor({state:'visible',timeout:30000})}catch{}const text=await page.locator('body').innerText(),commit=page.getByRole('button',{name:/اعتماد وكتابة/});const canCommit=await commit.count()>0&&await commit.isVisible().catch(()=>false)&&await commit.isEnabled().catch(()=>false);r.assertions.push(...scenarioAssertionPlan(scenario.id));r.observed={can_commit:canCommit,page_text_excerpt:text.slice(0,1000)};if(!canCommit){r.actual_status=/فشل|تعذر|خطأ|مرفوض|ناقص|غير صالح/i.test(text)?'rejected_or_reviewed':'reviewed';r.terminal_state=r.actual_status;r.after_state=await snapshot();assertNoFalseCommit({status:null,checkpoint:null,canCommit:false});assertZeroUnintendedMutation(r.before_state,r.after_state);r.negative_assertions.push('zero unintended canonical/invoice/movement/KPI mutation','no false committed state');return r}const wait=page.waitForResponse(x=>x.url().endsWith('/rest/v1/rpc/enqueue_report_execution_job')&&x.request().method()==='POST',{timeout:30000});await commit.click();const er=await wait;const eb=await er.text();assert.equal(er.ok(),true,`enqueue HTTP ${er.status()}: ${eb}`);const ep=JSON.parse(eb);r.job_id=String(ep?.id??ep?.[0]?.id??'');assert.match(r.job_id,/^[0-9a-f-]{36}$/i);let jobs=[];const deadline=Date.now()+120000;while(Date.now()<deadline){jobs=await rest('report_execution_jobs','id,company_id,source_hash,status,checkpoint,last_error,evidence',{id:r.job_id,company_id:tenantId});if(jobs.length===1&&['completed','failed'].includes(jobs[0].status))break;await new Promise(x=>setTimeout(x,1500))}assert.equal(jobs.length,1,'durable job readback missing');const job=jobs[0];r.persistence_readback={status:job.status,checkpoint:job.checkpoint,source_hash:job.source_hash};assertRenderedJob(job,tenantId);assertEvidenceProvenance(job,{tenant:tenantId});r.actual_status='committed_and_rendered';r.terminal_state='rendered';r.evidence_references.push(`report_execution_jobs:${r.job_id}`);r.after_state=await snapshot();r.dashboard=r.after_state.dashboard;assertTenantPeriodCurrency({dashboard:r.dashboard,tenant:tenantId,expectedCurrency:null,expectedPeriod:null});assert.ok(r.after_state.invoices.count>=r.before_state.invoices.count&&r.after_state.movements.count>=r.before_state.movements.count&&r.after_state.evidence.count>=r.before_state.evidence.count,'POST_COMMIT_NEGATIVE_STATE_DELTA');r.business_assertions.push('rendered lifecycle','DB readback','dashboard readback','tenant binding','provenance/evidence')}catch(e){r.failure=e instanceof Error?e.message:String(e);r.actual_status='failed'}finally{r.execution_end=new Date().toISOString()}return r}
try{await login();for(const s of scenarios){const input=inputs.get(s.id);assert.ok(input,`SCENARIO_INPUT_MISSING:${s.id}`);const r=await runOne(s,input);results.push(r);if(s.id==='duplicate-transactions'&&r.actual_status==='committed_and_rendered'){const second=await runOne({...s,id:'duplicate-transactions:second-run'},input);const di=second.after_state.invoices.count-second.before_state.invoices.count;const dm=second.after_state.movements.count-second.before_state.movements.count;assertDuplicateIdempotency(r,{status:second.actual_status,newCanonicalRows:dm,newInvoiceRows:di});assert.equal(di,0,'DUPLICATE_INVOICE_MUTATION');assert.equal(dm,0,'DUPLICATE_MOVEMENT_MUTATION');results.push(second)}}}finally{await page.close();await context.close();await browser.close()}
const primary=results.filter(x=>!x.scenario_id.endsWith(':second-run'));assert.equal(primary.length,scenarios.length,`SCENARIO_COUNT_MISMATCH:${primary.length}/${scenarios.length}`);const compact=primary.map(x=>buildCompactEvidence({scenario:x.scenario_id,sha:exactHead,runId:x.job_id,status:x.actual_status,terminalState:x.terminal_state,before:x.before_state,after:x.after_state,assertions:{business:x.business_assertions,negative:x.negative_assertions},evidenceRefs:x.evidence_references}));const artifact={exact_head:exactHead,generated_at:new Date().toISOString(),authenticated_runtime:true,tenant:tenantId,scenario_count:scenarios.length,results:primary,compact_evidence:compact,duplicate_followup:results.find(x=>x.scenario_id==='duplicate-transactions:second-run')||null};const canonical=JSON.stringify(artifact);artifact.artifact_hash=crypto.createHash('sha256').update(canonical).digest('hex');validateCompactArtifact(artifact,exactHead,12);await fs.writeFile(out,JSON.stringify(artifact,null,2));console.log(JSON.stringify({exact_head:exactHead,executed:primary.length,rendered:primary.filter(x=>x.actual_status==='committed_and_rendered').length,rejected_or_reviewed:primary.filter(x=>['reviewed','rejected_or_reviewed'].includes(x.actual_status)).length,failed:primary.filter(x=>x.actual_status==='failed').length,artifact_hash:artifact.artifact_hash},null,2));