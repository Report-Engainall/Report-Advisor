import fs from 'node:fs';
import path from 'node:path';
const root=process.cwd();
const files=['scripts/check-production-certification-contract.mjs','scripts/check-production-release-blockers.mjs','scripts/check-phase-m-certification-contract.mjs','scripts/check-evidence-provenance-chain.mjs','scripts/check-evidence-lineage-contract.mjs','scripts/check-phase11-e2e-performance-closure.mjs','supabase/migrations/20260825150000_phase_m_certification_bundle.sql'];
for(const f of files) if(!fs.existsSync(path.join(root,f))) throw new Error(`Missing certification component: ${f}`);
const text=files.map(f=>fs.readFileSync(path.join(root,f),'utf8')).join('\n').toLowerCase();
const checks={tenant:['tenant'],storage:['storage'],realtime:['realtime'],retrieval:['retrieval','source snapshot','sourcesnapshotid','evidence lineage'],backup:['backup'],migration:['migration'],artifact:['artifact'],rollback:['rollback'],security:['security'],continuous_trust:['continuous_trust','continuous trust']};
for(const [name,tokens] of Object.entries(checks)) if(!tokens.some(t=>text.includes(t.toLowerCase()))) throw new Error(`Certification chain missing: ${name}`);
for(const t of ['fail-closed','blocker','production']) if(!text.includes(t)) throw new Error(`Certification fail-closed invariant missing: ${t}`);
if(/grant\s+all\s+to\s+anon/i.test(text)) throw new Error('Unsafe anonymous certification grant detected');
console.log('Production certification chain: PASS');
