const roles={sku:['sku','item code','product code'],name:['name','item name','product name'],unit:['unit','uom'],date:['date','transaction date'],amount:['amount','price','debit','credit','balance'],currency:['currency','curr','العملة']};
function kind(v){if(v==null||v==='')return'empty';const s=String(v).trim();if(/^[-+]?\d+(?:[.,]\d+)?$/.test(s))return'number';if(/^\d{4}[-/]\d{1,2}[-/]\d{1,2}/.test(s)||/^\d{1,2}[-/]\d{1,2}[-/]\d{2,4}/.test(s))return'date';return'text';}
export function inferHeaderlessSchema(rows=[]){if(!rows.length)return{mapping:{},confidence:0,quarantine:true,reason:'empty'};const width=Math.max(...rows.map(r=>r.length));const mapping={};for(let c=0;c<width;c++){const vals=rows.map(r=>r[c]).filter(v=>v!==''&&v!=null);const kinds=vals.map(kind);if(kinds.length&&kinds.every(k=>k==='date'))mapping[c]='date';else if(kinds.length&&kinds.filter(k=>k==='number').length/kinds.length>=.8)mapping[c]='amount';else if(kinds.length&&kinds.filter(k=>k==='text').length/kinds.length>=.8)mapping[c]='name';}
const confidence=Object.keys(mapping).length/width;return{mapping,confidence,quarantine:confidence<.5};}
