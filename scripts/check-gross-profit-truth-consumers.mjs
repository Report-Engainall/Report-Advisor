import { readFileSync } from 'node:fs';
const read=p=>readFileSync(p,'utf8');const dashboard=read('src/pages/DashboardPage.tsx'),semantic=read('src/lib/semanticMetrics.ts'),canonical=read('src/lib/canonicalFinancialQueries.ts');const failures=[];for(const [t,n,m] of [[dashboard,'fetchCanonicalDashboardKPIs','Dashboard KPI must use canonical financial query'],[dashboard,'fetchCanonicalMonthlyTrend','Dashboard trend must use canonical financial query'],[dashboard,'fetchCanonicalTopCustomers','Dashboard customer revenue must use canonical line-item revenue'],[semantic,"formula:'SUM(sale_items.line_total)'",'net_sales semantic contract must use line-item revenue'],[canonical,'sale_items.line_total','canonical query must use line-item revenue'],[canonical,'sale_items.cost_price','canonical query must use line-item cost']])if(!t.includes(n))failures.push(m);if(/fetchDashboardKPIs|fetchMonthlyTrend|fetchTopCustomers/.test(dashboard))failures.push('Dashboard still imports legacy divergent financial consumers');if(/sales_invoices\.subtotal/.test(canonical))failures.push('Canonical financial query contains invoice subtotal as revenue');if(failures.length){console.error('GROSS_PROFIT_TRUTH_CONSUMER_GATE: FAIL');for(const f of failures)console.error(`- ${f}`);process.exit(1)}console.log('GROSS_PROFIT_TRUTH_CONSUMER_GATE: PASS');
