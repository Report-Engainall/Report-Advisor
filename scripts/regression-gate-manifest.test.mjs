import{strict as assert}from'node:assert';import{regressionGates,validateManifest}from'./regression-gate-manifest.mjs';const r=validateManifest();assert.equal(r.valid,true);assert.equal(r.unique,true);assert.ok(regressionGates.includes('integration-pipeline-simulation'));assert.ok(regressionGates.includes('production-gate-integrity'));console.log('Regression gate manifest tests PASS.');