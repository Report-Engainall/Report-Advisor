import assert from 'node:assert/strict';
import fs from 'node:fs';
const source=fs.readFileSync('src/lib/free-toolbox/queryPlanner.ts','utf8');
assert.match(source,/company_id/);
assert.match(source,/ALLOWED_TABLES/);
assert.match(source,/SAFE_COLUMNS/);
assert.match(source,/Math\.min\(Math\.max\(Math\.trunc\(input\.limit\?\?500\),1\),5000\)/);
assert.match(source,/filters=\{\.\.\.\(input\.filters\?\?\{\}\),company_id:input\.companyId\}/);
assert.match(source,/unsupported query table/);
assert.match(source,/invalid query date window/);
console.log('query planner contract: PASS');
