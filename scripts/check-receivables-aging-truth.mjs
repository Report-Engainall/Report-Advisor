import fs from 'node:fs';const m=fs.readFileSync('supabase/migrations/20260827160000_receivables_aging_truth.sql','utf8');for(const s of ['public.current_company_id()',"WHEN total IS NULL OR paid_amount IS NULL THEN 'INCOMPLETE'","WHEN due_date IS NULL THEN 'UNDATED'","NOT IN ('cancelled','canceled','void')"])if(!m.includes(s))throw new Error('Missing aging truth invariant: '+s);console.log('Receivables aging truth contract: PASS');