const numeric=v=>{const n=Number(String(v??'').replace(/,/g,''));return v!==''&&v!=null&&Number.isFinite(n)};
export function detectConflicts({reportType,fields={},rows=[]}){const conflicts=[];const roles=Object.values(fields);const dup=[...new Set(roles.filter((r,i)=>roles.indexOf(r)!==i))];if(dup.length)conflicts.push({code:'ROLE_COLLISION',roles:dup});if(reportType==='exchange_statement'&&fields.debit&&fields.credit){for(let i=0;i<rows.length;i++){const d=Number(String(rows[i][fields.debit]??0).replace(/,/g,'')),c=Number(String(rows[i][fields.credit]??0).replace(/,/g,''));if(numeric(rows[i][fields.debit])&&numeric(rows[i][fields.credit])&&d!==0&&c!==0)conflicts.push({code:'BOTH_DEBIT_CREDIT',row:i+1});}}if(reportType==='exchange_statement'&&fields.currency){const currencies=new Set(rows.map(r=>String(r[fields.currency]??'').trim()).filter(Boolean));if(currencies.size>1)conflicts.push({code:'MULTI_CURRENCY',currencies:[...currencies]});}return{valid:conflicts.length===0,conflicts};}
