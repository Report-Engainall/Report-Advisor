export function buildProvenance({sourceHash,sourceName,sourceType,textHash,engine,version,pageCount=0,blocks=[]}){const safeBlocks=Array.isArray(blocks)?blocks:[];return{source:{hash:sourceHash,name:sourceName,type:sourceType},artifact:{hash:textHash,format:'canonical-text',engine,version},pages:Number.isFinite(pageCount)&&pageCount>=0?pageCount:0,blocks:safeBlocks.filter((b)=>b&&typeof b==='object').map((b,i)=>({id:typeof b.id==='string'&&b.id.trim()?b.id.trim():`b${i+1}`,page:Number.isFinite(b.page)&&b.page>=1?b.page:null,kind:typeof b.kind==='string'&&b.kind.trim()?b.kind.trim():'text',sourceRef:typeof b.sourceRef==='string'&&b.sourceRef.trim()?b.sourceRef.trim():null,start:Number.isFinite(b.start)&&b.start>=0?b.start:null,end:Number.isFinite(b.end)&&b.end>=0?b.end:null}))};}
export function validateFidelity({sourceHash,textHash,extractedText,provenance}){const errors=[];if(typeof sourceHash!=='string'||!sourceHash.trim()||provenance?.source?.hash!==sourceHash)errors.push('SOURCE_PROVENANCE_MISMATCH');if(typeof textHash!=='string'||!textHash.trim()||provenance?.artifact?.hash!==textHash)errors.push('ARTIFACT_PROVENANCE_MISMATCH');if(typeof extractedText!=='string'||!extractedText.trim())errors.push('EMPTY_CANONICAL_TEXT');if(typeof provenance?.artifact?.engine!=='string'||!provenance.artifact.engine.trim())errors.push('EXTRACTION_ENGINE_MISSING');return{valid:errors.length===0,errors};}
