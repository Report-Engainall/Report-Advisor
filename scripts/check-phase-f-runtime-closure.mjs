import fs from 'node:fs'; import path from 'node:path';
const root=process.cwd(); const files=['scripts/check-operational-resilience-contract.mjs','scripts/check-release-resilience-manifest.mjs','scripts/check-continuous-trust-contract.mjs','scripts/phase-f-live-resilience-probes.mjs','.github/workflows/phase-f-live-resilience.yml','supabase/migrations/20260825050000_operational_resilience_trust.sql','supabase/migrations/20260825090000_continuous_trust_autonomous_ops.sql'];
for(const file of files) if(!fs.existsSync(path.join(root,file))) throw new Error(`Phase F runtime closure blocker: ${file}`);
const migration=fs.readFileSync(path.join(root,'supabase/migrations/20260825050000_operational_resilience_trust.sql'),'utf8'); for(const t of ['ENABLE ROW LEVEL SECURITY','company_id = public.current_company_id()','REVOKE ALL ON TABLE','expires_at > now()','blocker_count = 0']) if(!migration.includes(t)) throw new Error(`Phase F security invariant missing: ${t}`);
const workflow=fs.readFileSync(path.join(root,'.github/workflows/phase-f-live-resilience.yml'),'utf8'); for(const t of ['phase-f-live-resilience','npm run test:operational-resilience','phase-f-live-resilience-probes.mjs']) if(!workflow.includes(t)) throw new Error(`Phase F workflow invariant missing: ${t}`);
console.log('Phase F runtime closure contract: PASS');