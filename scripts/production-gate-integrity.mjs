export function productionGate({extraction,fidelity,quality,schema,mapping,semantic,arithmetic,reconciliation,evidence}){const checks={extraction:!!extraction?.valid,fidelity:!!fidelity?.valid,quality:(quality?.score??0)>=.9,schema:!!schema?.valid,mapping:!!mapping?.valid,semantic:semantic?.valid!==false,arithmetic:arithmetic?.valid!==false,reconciliation:reconciliation?.valid!==false,evidence:!!evidence?.valid};const failed=Object.entries(checks).filter(([,v])=>!v).map(([k])=>k);return{ready:failed.length===0,checks,failed};}
