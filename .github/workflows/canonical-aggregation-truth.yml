name: canonical-aggregation-truth

on:
  pull_request:

jobs:
  canonical-aggregation-truth:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: node scripts/canonical-profitability-null-truth.test.mjs
