name: direct-truth-writers
on:
  pull_request:
    branches: [main]
  workflow_dispatch:
jobs:
  contract:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify direct truth writer privilege boundary
        run: node scripts/check-direct-truth-writers.mjs
