name: certification-evidence-writer-boundary
on:
  pull_request:
    branches: [main]
  workflow_dispatch:
jobs:
  contract:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify certification evidence writer boundary
        run: node scripts/check-certification-evidence-writer-boundary.mjs
