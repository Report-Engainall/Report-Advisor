import { useEffect,useState,useCallback } from 'react';
import { DollarSign,ShoppingCart,TrendingUp,Users,Package,AlertTriangle,Brain,ArrowLeftRight,Wallet,Receipt,RefreshCw,CalendarRange,CheckCircle2,Sparkles } from 'lucide-react';
import { KPICard } from '@/components/ui/KPICard';
import { Card,CardHeader,CardBody } from '@/components/ui/Card';
import { Badge,SeverityBadge,PriorityBadge } from '@/components/ui/Badge';
import { LoadingState,ErrorState } from '@/components/ui/States';
import { TrendChart,CategoryPieChart,HorizontalBarChart } from '@/components/ui/Charts';
import { fetchDashboardSnapshot,fetchDashboardIntelligence } from '@/lib/dashboard-canonical';
import { formatCurrency,relativeTime } from '@/lib/format';
import type { Recommendation,Alert } from '@/lib/types';
import type { DashboardKPIs,MonthlyTrend,TopEntity,CategoryBreakdown,AgingDashboard } from '@/lib/dashboard-canonical';
import { Link } from 'react-router-dom';
const TREND_RANGES=[{value:3,label:'3 أشهر'},{value:6,label:'6 أشهر'},{value:12,label:'12 شهرًا'}] as const;
export function DashboardPage(){
 const [kpis,setKpis]=useState<DashboardKPIs|null>(null),[trend,setTrend]=useState<MonthlyTrend[]>([]),[topCustomers,setTopCustomers]=useState<TopEntity[]>([]),[topProducts,setTopProducts]=useState<TopEntity[]>([]),[categories,setCategories]=useState<CategoryBreakdown[]>([]),[recommendations,setRecommendations]=useState<Recommendation[]>([]),[alerts,setAlerts]=useState<Alert[]>([]),[aging,setAging]=useState<AgingDashboard|null>(null),[trendMonths,setTrendMonths]=useState(6),[loading,setLoading]=useState(true),[refreshing,setRefreshing]=useState(false),[error,setError]=useState<string|null>(null);
 const load=useCallback(async(silent=false)=>{try{if(silent)setRefreshing(true);else setLoading(true);setError(null);const [{kpis:k,trend:t,topCustomers:tc,topProducts:tp,categories:cat,aging:ag},intelligence]=await Promise.all([fetchDashboardSnapshot(trendMonths),fetchDashboardIntelligence()]);setKpis(k);setTrend(t);setTopCustomers(tc.slice(0,5));setTopProducts(tp.slice(0,5));setCategories(cat);setAging(ag);setRecommendations(intelligence.recommendations);setAlerts(intelligence.alerts);}catch(e:unknown){setError(e instanceof Error?e.message:'فشل تحميل البيانات');}finally{setLoading(false);setRefreshing(false);}},[trendMonths]);
 useEffect(()=>{void load();},[load]);
 if(loading)return <LoadingState message="جارٍ تحميل لوحة القيادة..."/>;if(error)return <ErrorState message={error} onRetry={()=>void load()}/>;if(!kpis||!aging)return null;
 const categoryDisplay=categories.map(c=>({...c,name:c.categoryStatus==='UNKNOWN'?'UNKNOWN':c.name??'UNKNOWN'}));
 const dataStatusLabel=kpis.status==='INSUFFICIENT_DATA'?'بيانات غير كافية':'بيانات محسوبة من المصدر';
 return <div className="space-y-6 animate-fade-in">
  <header className="hero-gradient relative overflow-hidden rounded-[2rem] p-6 text-white shadow-elevated lg:p-8"><div className="pointer-events-none absolute -left-16 -top-20 h-56 w-56 rounded-full bg-cyan-400/20 blur-3xl"/><div className="pointer-events-none absolute bottom-[-5rem] right-1/3 h-48 w-48 rounded-full bg-indigo-400/20 blur-3xl"/><div className="relative flex flex-col gap-5 lg:flex-row lg:items-end lg:justify-between"><div><div className="flex items-center gap-2 text-sm font-semibold text-cyan-200"><Sparkles size={17}/> Executive Intelligence</div><div className="mt-2 flex items-center gap-2"><h1 className="text-3xl font-black tracking-tight lg:text-4xl">لوحة القيادة</h1><span className="inline-flex items-center gap-1 rounded-full bg-emerald-400/15 px-2.5 py-1 text-[11px] font-semibold text-emerald-100"><CheckCircle2 size={13}/>{dataStatusLabel}</span></div><p className="mt-2 max-w-2xl text-sm leading-7 text-slate-200">نظرة تنفيذية على الأداء والذمم والمخزون والأولويات — من البيانات المحسوبة من المصدر، دون أرقام تجميلية.</p></div><div className="flex flex-wrap items-center gap-2"><div className="flex items-center gap-1 rounded-xl border border-white/10 bg-white/10 p-1 backdrop-blur" aria-label="الفترة الزمنية للرسم"><CalendarRange size={16} className="mx-2 text-slate-300"/>{TREND_RANGES.map(r=><button key={r.value} type="button" onClick={()=>setTrendMonths(r.value)} className={`rounded-lg px-3 py-1.5 text-xs font-bold transition ${trendMonths===r.value?'bg-white text-primary-800 shadow-sm':'text-white/80 hover:bg-white/10'}`} aria-pressed={trendMonths===r.value}>{r.label}</button>)}</div><button type="button" onClick={()=>void load(true)} disabled={refreshing} className="inline-flex items-center gap-2 rounded-xl border border-white/15 bg-white/10 px-4 py-2.5 text-sm font-bold text-white backdrop-blur hover:bg-white/20"><RefreshCw size={16}/><span className="hidden sm:inline">تحديث</span></button></div></div></header>
  <div className="grid grid-cols-2 gap-4 lg:grid-cols-4"><KPICard label="إجمالي المبيعات" value={kpis.totalSales} format="currency" icon={<DollarSign size={16}/>} status={kpis.status}/><KPICard label="إجمالي الربح" value={kpis.grossProfit} format="currency" icon={<TrendingUp size={16}/>} status={kpis.status} hint={kpis.grossMargin===null?undefined:`هامش: ${kpis.grossMargin.toFixed(1)}%`}/><KPICard label="الذمم المدينة" value={kpis.totalReceivables} format="currency" icon={<Receipt size={16}/>} status={kpis.status}/><KPICard label="قيمة المخزون" value={kpis.inventoryValue} format="currency" icon={<Package size={16}/>} status={kpis.status}/><KPICard label="عدد العملاء" value={kpis.totalCustomers} format="number" icon={<Users size={16}/>} status="CONFIRMED"/><KPICard label="عدد المنتجات" value={kpis.totalProducts} format="number" icon={<Package size={16}/>} status="CONFIRMED"/><KPICard label="عدد الفواتير" value={kpis.invoiceCount} format="number" icon={<ShoppingCart size={16}/>} status={kpis.status}/><KPICard label="معدل التحصيل" value={kpis.collectionRate} format="percent" icon={<Wallet size={16}/>} status={kpis.status}/></div>
  <div className="grid grid-cols-1 gap-4 lg:grid-cols-3"><Card className="lg:col-span-2 card-hover"><CardHeader title="اتجاه المبيعات والربح" subtitle={`آخر ${trendMonths} أشهر`} action={<Badge variant="primary">تفاعلي</Badge>}/><CardBody>{trend.some(x=>x.status==='CALCULATED')?<TrendChart data={trend}/>:<p className="py-12 text-center text-sm text-ink-400">لا توجد بيانات اتجاه قابلة للحساب</p>}</CardBody></Card><Card className="card-hover"><CardHeader title="توزيع المبيعات حسب الفئة" subtitle="الحقيقة المصنفة من الخادم"/><CardBody>{categoryDisplay.length?<CategoryPieChart data={categoryDisplay}/>:<p className="py-12 text-center text-sm text-ink-400">لا توجد بيانات فئات</p>}</CardBody></Card></div>
  <div className="grid grid-cols-1 gap-4 lg:grid-cols-2"><Card className="card-hover"><CardHeader title="مركز الانتباه"/><CardBody><div className="space-y-3">{alerts.slice(0,4).map(a=><div key={a.id} className="flex items-start gap-3 rounded-xl bg-gradient-to-l from-warning-50/70 to-white p-3"><div><SeverityBadge severity={a.severity}/></div><div className="min-w-0 flex-1"><span className="text-sm font-semibold text-ink-800">{a.title}</span>{a.description&&<p className="truncate text-xs text-ink-500">{a.description}</p>}</div><span className="text-[11px] text-ink-400">{relativeTime(a.created_at)}</span></div>)}{alerts.length===0&&<p className="py-4 text-center text-sm text-ink-400">لا توجد تنبيهات</p>}</div></CardBody></Card><Card className="card-hover"><CardHeader title="التوصيات النشطة"/><CardBody><div className="space-y-3">{recommendations.filter(r=>r.status==='new'||r.status==='accepted').slice(0,4).map(r=><div key={r.id} className="flex items-start gap-3 rounded-xl bg-gradient-to-l from-primary-50/70 to-white p-3"><Brain size={16} className="mt-0.5 text-primary-600"/><div><span className="text-sm font-semibold text-ink-800">{r.title}</span><PriorityBadge priority={r.priority}/>{r.description&&<p className="text-xs text-ink-500">{r.description}</p>}</div></div>)}{recommendations.length===0&&<p className="py-4 text-center text-sm text-ink-400">لا توجد توصيات</p>}</div></CardBody></Card></div>
  <div className="grid grid-cols-1 gap-4 lg:grid-cols-3"><Card className="card-hover"><CardHeader title="أفضل العملاء"/><CardBody><HorizontalBarChart data={topCustomers} dataKey="value" nameKey="name" height={220}/></CardBody></Card><Card className="card-hover"><CardHeader title="أفضل المنتجات"/><CardBody><HorizontalBarChart data={topProducts} dataKey="value" nameKey="name" height={220}/></CardBody></Card><Card className="card-hover"><CardHeader title="تحليل أعمار الذمم" subtitle={aging.status==='CALCULATED'&&aging.totalAmount!==null?`الإجمالي: ${formatCurrency(aging.totalAmount)}`:aging.status==='NO_DATA'?'لا توجد بيانات ذمم':'بيانات غير كافية'}/><CardBody><div className="space-y-3">{aging.rows.map(b=><div key={b.bucket} className="flex items-center justify-between border-b border-ink-100 py-2"><span className="text-xs font-medium text-ink-600">{b.bucket}</span><span className="text-xs text-ink-500">{b.amount===null?'UNKNOWN':formatCurrency(b.amount)} · {b.count} فاتورة</span></div>)}{aging.unknownRows>0&&<p className="text-xs text-ink-400">UNKNOWN: {aging.unknownRows} فاتورة بلا تاريخ استحقاق</p>}</div></CardBody></Card></div>
  <Card className="card-hover"><CardHeader title="إجراءات سريعة"/><CardBody><div className="grid grid-cols-2 gap-3 sm:grid-cols-3 lg:grid-cols-6">{[['/import',ArrowLeftRight,'استيراد بيانات'],['/reports',Receipt,'عرض التقارير'],['/analytics',TrendingUp,'التحليلات'],['/intelligence',Brain,'المساعد الذكي'],['/customers',Users,'العملاء'],['/inventory',Package,'المخزون']].map(([path,Icon,label])=>{const I=Icon as typeof ArrowLeftRight;return <Link key={path as string} to={path as string} className="group flex flex-col items-center gap-2 rounded-2xl border border-ink-100 bg-gradient-to-br from-white to-primary-50/40 p-4 transition-all hover:-translate-y-0.5 hover:border-primary-200 hover:shadow-card-hover"><I size={20} className="text-primary-600 transition-transform group-hover:scale-110"/><span className="text-xs font-semibold">{label as string}</span></Link>})}</div></CardBody></Card>
 </div>;
}
