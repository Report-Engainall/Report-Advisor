import { useEffect, useState, useCallback } from 'react';
import { Database, ShieldCheck, CheckCircle2, AlertTriangle, Users, Package, Warehouse, BarChart3 } from 'lucide-react';
import { Card, CardHeader, CardBody } from '@/components/ui/Card';
import { Badge } from '@/components/ui/Badge';
import { PageHeader, LoadingState, ErrorState, EmptyState } from '@/components/ui/States';
import { DataTable } from '@/components/ui/DataTable';
import { fetchDataQualityDatasets } from '@/lib/data-quality-queries';
import { formatNumber } from '@/lib/format';

interface QualityIssue { entity: string; field: string; issue: string; count: number; severity: 'critical' | 'warning' | 'info'; }
interface EntityQuality { name: string; total: number; issues: number; score: number; icon: 'users' | 'package' | 'warehouse' | 'receipt'; }
function scoreColor(score: number): string { if (score >= 90) return 'text-success-600'; if (score >= 70) return 'text-warning-600'; return 'text-danger-600'; }
function scoreBg(score: number): string { if (score >= 90) return 'bg-success-500'; if (score >= 70) return 'bg-warning-500'; return 'bg-danger-500'; }
function severityBadge(sev: string): string { if (sev === 'critical') return 'danger'; if (sev === 'warning') return 'warning'; return 'neutral'; }

export function DataQualityPage() {
  const [loading, setLoading] = useState(true); const [error, setError] = useState<string | null>(null); const [issues, setIssues] = useState<QualityIssue[]>([]); const [entities, setEntities] = useState<EntityQuality[]>([]); const [overallScore, setOverallScore] = useState(0);
  const load = useCallback(async () => { try { setLoading(true); setError(null); const { customers, products, invoices, balances } = await fetchDataQualityDatasets(); const allIssues: QualityIssue[] = []; const custTotal = customers.length; let custIssues = 0; const custMissingName = customers.filter(c => !String(c.name || '').trim()).length; const custMissingPhone = customers.filter(c => !String(c.phone || '').trim()).length; const custMissingCode = customers.filter(c => !String(c.code || '').trim()).length; const custDupNames = new Map<string, number>(); customers.forEach(c => { const n = String(c.name || '').trim(); if (n) custDupNames.set(n, (custDupNames.get(n) || 0) + 1); }); const custDuplicateNames = Array.from(custDupNames.values()).filter(v => v > 1).reduce((s, v) => s + v, 0); custIssues += custMissingName + custMissingPhone + custMissingCode + custDuplicateNames; if (custMissingName) allIssues.push({ entity: 'العملاء', field: 'الاسم', issue: 'اسم فارغ', count: custMissingName, severity: 'critical' }); if (custMissingPhone) allIssues.push({ entity: 'العملاء', field: 'الهاتف', issue: 'هاتف فارغ', count: custMissingPhone, severity: 'warning' }); if (custMissingCode) allIssues.push({ entity: 'العملاء', field: 'الكود', issue: 'كود فارغ', count: custMissingCode, severity: 'info' }); if (custDuplicateNames) allIssues.push({ entity: 'العملاء', field: 'الاسم', issue: 'أسماء مكررة', count: custDuplicateNames, severity: 'warning' }); const custScore = custTotal > 0 ? Math.round(((custTotal - custIssues) / custTotal) * 100) : 100; const prodTotal = products.length; let prodIssues = 0; const prodMissingSku = products.filter(p => !String(p.sku || '').trim()).length; const prodMissingName = products.filter(p => !String(p.name || '').trim()).length; const prodZeroPrice = products.filter(p => Number(p.selling_price) <= 0).length; const prodNegPrice = products.filter(p => Number(p.cost_price) < 0 || Number(p.selling_price) < 0).length; const prodDupSku = new Map<string, number>(); products.forEach(p => { const s = String(p.sku || '').trim(); if (s) prodDupSku.set(s, (prodDupSku.get(s) || 0) + 1); }); const prodDuplicateSku = Array.from(prodDupSku.values()).filter(v => v > 1).reduce((s, v) => s + v, 0); prodIssues += prodMissingSku + prodMissingName + prodZeroPrice + prodNegPrice + prodDuplicateSku; if (prodMissingSku) allIssues.push({ entity: 'المنتجات', field: 'SKU', issue: 'SKU فارغ', count: prodMissingSku, severity: 'critical' }); if (prodMissingName) allIssues.push({ entity: 'المنتجات', field: 'الاسم', issue: 'اسم فارغ', count: prodMissingName, severity: 'critical' }); if (prodZeroPrice) allIssues.push({ entity: 'المنتجات', field: 'السعر', issue: 'سعر صفري', count: prodZeroPrice, severity: 'warning' }); if (prodNegPrice) allIssues.push({ entity: 'المنتجات', field: 'السعر', issue: 'سعر سالب', count: prodNegPrice, severity: 'critical' }); if (prodDuplicateSku) allIssues.push({ entity: 'المنتجات', field: 'SKU', issue: 'SKU مكرر', count: prodDuplicateSku, severity: 'critical' }); const prodScore = prodTotal > 0 ? Math.round(((prodTotal - prodIssues) / prodTotal) * 100) : 100; const invTotal = invoices.length; let invIssues = 0; const invZeroTotal = invoices.filter(i => Number(i.total) <= 0).length; const invNegTotal = invoices.filter(i => Number(i.total) < 0).length; const invPaidExceeds = invoices.filter(i => Number(i.paid_amount) > Number(i.total)).length; const invMissingCustomer = invoices.filter(i => !i.customer_id).length; const invMissingDate = invoices.filter(i => !i.invoice_date).length; const invDupNumber = new Map<string, number>(); invoices.forEach(i => { const n = String(i.invoice_number || '').trim(); if (n) invDupNumber.set(n, (invDupNumber.get(n) || 0) + 1); }); const invDuplicateNum = Array.from(invDupNumber.values()).filter(v => v > 1).reduce((s, v) => s + v, 0); invIssues += invZeroTotal + invNegTotal + invPaidExceeds + invMissingCustomer + invMissingDate + invDuplicateNum; if (invZeroTotal) allIssues.push({ entity: 'الفواتير', field: 'الإجمالي', issue: 'إجمالي صفري', count: invZeroTotal, severity: 'warning' }); if (invNegTotal) allIssues.push({ entity: 'الفواتير', field: 'الإجمالي', issue: 'إجمالي سالب', count: invNegTotal, severity: 'critical' }); if (invPaidExceeds) allIssues.push({ entity: 'الفواتير', field: 'المدفوع', issue: 'مدفوع يتجاوز الإجمالي', count: invPaidExceeds, severity: 'critical' }); if (invMissingCustomer) allIssues.push({ entity: 'الفواتير', field: 'العميل', issue: 'عميل فارغ', count: invMissingCustomer, severity: 'critical' }); if (invMissingDate) allIssues.push({ entity: 'الفواتير', field: 'التاريخ', issue: 'تاريخ فارغ', count: invMissingDate, severity: 'critical' }); if (invDuplicateNum) allIssues.push({ entity: 'الفواتير', field: 'رقم الفاتورة', issue: 'أرقام مكررة', count: invDuplicateNum, severity: 'warning' }); const invScore = invTotal > 0 ? Math.round(((invTotal - invIssues) / invTotal) * 100) : 100; const balTotal = balances.length; let balIssues = 0; const balNegQty = balances.filter(b => Number(b.quantity) < 0).length; const balNegCost = balances.filter(b => Number(b.unit_cost) < 0).length; const balMissingProduct = balances.filter(b => !b.product_id).length; balIssues += balNegQty + balNegCost + balMissingProduct; if (balNegQty) allIssues.push({ entity: 'المخزون', field: 'الكمية', issue: 'كمية سالبة', count: balNegQty, severity: 'critical' }); if (balNegCost) allIssues.push({ entity: 'المخزون', field: 'التكلفة', issue: 'تكلفة سالبة', count: balNegCost, severity: 'critical' }); if (balMissingProduct) allIssues.push({ entity: 'المخزون', field: 'المنتج', issue: 'منتج فارغ', count: balMissingProduct, severity: 'critical' }); const balScore = balTotal > 0 ? Math.round(((balTotal - balIssues) / balTotal) * 100) : 100; setEntities([{ name: 'العملاء', total: custTotal, issues: custIssues, score: custScore, icon: 'users' },{ name: 'المنتجات', total: prodTotal, issues: prodIssues, score: prodScore, icon: 'package' },{ name: 'الفواتير', total: invTotal, issues: invIssues, score: invScore, icon: 'receipt' },{ name: 'المخزون', total: balTotal, issues: balIssues, score: balScore, icon: 'warehouse' }]); setIssues(allIssues.sort((a, b) => b.count - a.count)); const totalRecords = custTotal + prodTotal + invTotal + balTotal; const totalIssues = custIssues + prodIssues + invIssues + balIssues; setOverallScore(totalRecords > 0 ? Math.round(((totalRecords - totalIssues) / totalRecords) * 100) : 100); } catch (e: any) { setError(e.message || 'فشل تحميل بيانات الجودة'); } finally { setLoading(false); } }, []);
  useEffect(() => { load(); }, [load]);
  if (loading) return <LoadingState message="جارٍ فحص جودة البيانات..." />; if (error) return <ErrorState message={error} onRetry={load} />;
  const iconMap: Record<string, any> = { users: Users, package: Package, receipt: BarChart3, warehouse: Warehouse }; const totalRecords = entities.reduce((s, e) => s + e.total, 0); const totalIssues = entities.reduce((s, e) => s + e.issues, 0);
  return <div className="space-y-6 animate-fade-in"><PageHeader title="جودة البيانات" subtitle="فحص شمولية ودقة البيانات عبر جميع الكيانات" /><Card className="bg-gradient-to-br from-ink-50 to-white"><CardBody><div className="flex flex-col lg:flex-row items-center gap-6"><div className="relative w-32 h-32 flex-shrink-0"><svg className="w-full h-full -rotate-90" viewBox="0 0 120 120"><circle cx="60" cy="60" r="52" fill="none" stroke="currentColor" strokeWidth="10" className="text-ink-100" /><circle cx="60" cy="60" r="52" fill="none" stroke="currentColor" strokeWidth="10" className={overallScore >= 90 ? 'text-success-500' : overallScore >= 70 ? 'text-warning-500' : 'text-danger-500'} strokeDasharray={`${(overallScore / 100) * 327} 327`} strokeLinecap="round" /></svg><div className="absolute inset-0 flex flex-col items-center justify-center"><span className={`text-3xl font-bold ${scoreColor(overallScore)}`}>{overallScore}%</span><span className="text-xs text-ink-400 mt-1">الدرجة الإجمالية</span></div></div><div className="flex-1 grid grid-cols-2 sm:grid-cols-4 gap-4 w-full"><div className="text-center p-3 rounded-lg bg-ink-50"><Database className="mx-auto text-primary-500 mb-1" size={20} /><div className="text-xl font-bold text-ink-900">{formatNumber(totalRecords)}</div><div className="text-xs text-ink-500">إجمالي السجلات</div></div><div className="text-center p-3 rounded-lg bg-ink-50"><CheckCircle2 className="mx-auto text-success-500 mb-1" size={20} /><div className="text-xl font-bold text-success-600">{formatNumber(totalRecords - totalIssues)}</div><div className="text-xs text-ink-500">سجلات سليمة</div></div><div className="text-center p-3 rounded-lg bg-ink-50"><AlertTriangle className="mx-auto text-warning-500 mb-1" size={20} /><div className="text-xl font-bold text-warning-600">{formatNumber(totalIssues)}</div><div className="text-xs text-ink-500">مشاكل مكتشفة</div></div><div className="text-center p-3 rounded-lg bg-ink-50"><ShieldCheck className="mx-auto text-primary-500 mb-1" size={20} /><div className="text-xl font-bold text-ink-900">{entities.length}</div><div className="text-xs text-ink-500">كيانات مفحوصة</div></div></div></div></CardBody></Card><div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4">{entities.map(e => { const Icon = iconMap[e.icon] || Database; return <Card key={e.name}><CardBody><div className="flex items-center gap-3 mb-3"><div className="w-10 h-10 rounded-lg bg-primary-50 text-primary-600 flex items-center justify-center"><Icon size={18} /></div><div><div className="font-semibold text-sm text-ink-800">{e.name}</div><div className="text-xs text-ink-400">{formatNumber(e.total)} سجل</div></div></div><div className="flex items-center justify-between mb-2"><span className="text-xs text-ink-500">الدرجة</span><span className={`text-lg font-bold ${scoreColor(e.score)}`}>{e.score}%</span></div><div className="h-2 bg-ink-100 rounded-full overflow-hidden"><div className={`h-full ${scoreBg(e.score)} rounded-full transition-all duration-500`} style={{ width: `${e.score}%` }} /></div>{e.issues > 0 ? <div className="text-xs text-warning-600 mt-2">{formatNumber(e.issues)} مشكلة</div> : <div className="text-xs text-success-600 mt-2">لا توجد مشاكل</div>}</CardBody></Card>})}</div><Card><CardHeader title="المشاكل المكتشفة" subtitle="تفاصيل مشاكل جودة البيانات" />{issues.length === 0 ? <CardBody><EmptyState icon={<CheckCircle2 size={32} />} title="لا توجد مشاكل في جودة البيانات" message="جميع السجلات سليمة ومكتملة" /></CardBody> : <DataTable columns={[{ key: 'entity', label: 'الكيان', render: (r: QualityIssue) => <span className="font-medium text-ink-800">{r.entity}</span> },{ key: 'field', label: 'الحقل', render: (r: QualityIssue) => r.field },{ key: 'issue', label: 'المشكلة', render: (r: QualityIssue) => r.issue },{ key: 'count', label: 'العدد', align: 'center', render: (r: QualityIssue) => <span className="font-semibold text-ink-800">{formatNumber(r.count)}</span> },{ key: 'severity', label: 'الخطورة', align: 'center', render: (r: QualityIssue) => { const sevMap: any = { critical: 'حرجة', warning: 'تحذير', info: 'معلومة' }; return <Badge variant={severityBadge(r.severity) as any}>{sevMap[r.severity] || r.severity}</Badge>; }}]} data={issues} emptyMessage="لا توجد مشاكل" />}</Card></div>;
}
