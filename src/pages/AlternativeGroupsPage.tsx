import { useEffect, useState } from 'react';
import { Plus, Save, Trash2, UsersRound, RefreshCw, AlertTriangle } from 'lucide-react';
import { supabase, COMPANY_ID } from '@/lib/supabase';
import { validateAlternativeGroupIsolation, type TenantScopedMember } from '@/lib/free-toolbox/alternative-group-security';

type Group={id:string;name:string;description:string|null;base_unit:string;is_active:boolean;created_at:string};
type Member={id:string;group_id:string;sku:string;conversion_factor:number};

export function AlternativeGroupsPage(){
 const [groups,setGroups]=useState<Group[]>([]); const [members,setMembers]=useState<Member[]>([]); const [selected,setSelected]=useState<string|null>(null);
 const [name,setName]=useState(''); const [description,setDescription]=useState(''); const [baseUnit,setBaseUnit]=useState('unit'); const [sku,setSku]=useState(''); const [factor,setFactor]=useState('1');
 const [loading,setLoading]=useState(true); const [saving,setSaving]=useState(false); const [error,setError]=useState('');
 const load=async()=>{setLoading(true);setError(''); const [g,m]=await Promise.all([supabase.from('alternative_item_groups').select('*').eq('company_id',COMPANY_ID).order('created_at',{ascending:false}),supabase.from('alternative_item_group_members').select('*').eq('company_id',COMPANY_ID)]); if(g.error||m.error)setError((g.error||m.error)?.message||'تعذر تحميل المجموعات'); setGroups((g.data||[]) as Group[]);setMembers((m.data||[]) as Member[]);setLoading(false)};
 useEffect(()=>{load()},[]);
 const reset=()=>{setSelected(null);setName('');setDescription('');setBaseUnit('unit');setSku('');setFactor('1');setError('')};
 const saveGroup=async()=>{if(!name.trim()){setError('اسم المجموعة مطلوب');return} setSaving(true);setError(''); const {data,error:e}=await supabase.from('alternative_item_groups').insert({company_id:COMPANY_ID,name:name.trim(),description:description.trim()||null,base_unit:baseUnit.trim()||'unit'}).select().single(); if(e){setError(e.message);setSaving(false);return} setGroups(v=>[data as Group,...v]);setSelected(data.id);setName('');setDescription('');setSaving(false)};
 const addMember=async()=>{if(!selected||!sku.trim()){setError('اختر مجموعة وأدخل SKU');return} const numeric=Number(factor); if(!Number.isFinite(numeric)||numeric<=0){setError('معامل التحويل يجب أن يكون أكبر من صفر');return} const candidate:TenantScopedMember={tenantId:COMPANY_ID,sku:sku.trim(),groupId:selected}; const existing=members.filter(m=>m.sku===candidate.sku&&m.group_id!==selected).map(m=>({tenantId:COMPANY_ID,sku:m.sku,groupId:m.group_id})); const issues=validateAlternativeGroupIsolation([...existing,candidate]); if(issues.length){setError('هذا SKU مرتبط بمجموعة بديلة أخرى داخل الشركة');return} setSaving(true);const {data,error:e}=await supabase.from('alternative_item_group_members').insert({company_id:COMPANY_ID,group_id:selected,sku:sku.trim(),conversion_factor:numeric}).select().single(); if(e){setError(e.message);setSaving(false);return}setMembers(v=>[...v,data as Member]);setSku('');setFactor('1');setSaving(false)};
 const removeMember=async(id:string)=>{setSaving(true);const {error:e}=await supabase.from('alternative_item_group_members').delete().eq('id',id).eq('company_id',COMPANY_ID);if(e)setError(e.message);else setMembers(v=>v.filter(m=>m.id!==id));setSaving(false)};
 const activeMembers=members.filter(m=>m.group_id===selected); const selectedGroup=groups.find(g=>g.id===selected);
 return <div dir="rtl" className="space-y-6">
  <div className="flex items-center justify-between"><div><h1 className="text-2xl font-bold text-ink-900">مجموعات الأصناف البديلة</h1><p className="text-sm text-ink-500 mt-1">اجمع الأصناف المتكافئة لتصبح قرارات المخزون والطلب على مستوى المجموعة.</p></div><button onClick={load} className="btn-secondary"><RefreshCw size={16}/> تحديث</button></div>
  {error&&<div className="rounded-xl border border-red-200 bg-red-50 p-4 text-sm text-red-700 flex gap-2"><AlertTriangle size={18}/>{error}</div>}
  <div className="grid lg:grid-cols-[360px_1fr] gap-5">
   <section className="card p-5 space-y-4"><div className="flex items-center gap-2 font-semibold"><Plus size={18}/> إنشاء مجموعة</div><input className="input" placeholder="اسم المجموعة" value={name} onChange={e=>setName(e.target.value)}/><textarea className="input min-h-20" placeholder="وصف اختياري" value={description} onChange={e=>setDescription(e.target.value)}/><input className="input" placeholder="الوحدة الأساسية" value={baseUnit} onChange={e=>setBaseUnit(e.target.value)}/><button disabled={saving} onClick={saveGroup} className="btn-primary w-full"><Save size={16}/> حفظ المجموعة</button><button onClick={reset} className="btn-secondary w-full">تفريغ النموذج</button></section>
   <section className="card overflow-hidden"><div className="p-5 border-b border-ink-100 flex items-center justify-between"><div><h2 className="font-semibold">المجموعات الحالية</h2><p className="text-xs text-ink-400">{loading?'جاري التحميل…':`${groups.length} مجموعة`}</p></div></div><div className="divide-y divide-ink-100">{groups.map(g=><button key={g.id} onClick={()=>setSelected(g.id)} className={`w-full text-right p-4 hover:bg-ink-50 ${selected===g.id?'bg-primary-50':''}`}><div className="font-medium">{g.name}</div><div className="text-xs text-ink-400 mt-1">{g.description||'بدون وصف'} · {members.filter(m=>m.group_id===g.id).length} أصناف</div></button>)}{!groups.length&&!loading&&<div className="p-8 text-center text-ink-400">لا توجد مجموعات بعد</div>}</div></section>
  </div>
  {selectedGroup&&<section className="card p-5 space-y-5"><div><h2 className="text-lg font-semibold">{selectedGroup.name}</h2><p className="text-sm text-ink-500">الوحدة الأساسية: {selectedGroup.base_unit}</p></div><div className="grid md:grid-cols-[1fr_180px_auto] gap-3"><input className="input" placeholder="SKU / رقم الصنف" value={sku} onChange={e=>setSku(e.target.value)}/><input className="input" type="number" min="0.000001" step="any" placeholder="معامل التحويل" value={factor} onChange={e=>setFactor(e.target.value)}/><button disabled={saving} onClick={addMember} className="btn-primary"><Plus size={16}/> إضافة</button></div><div className="overflow-x-auto"><table className="w-full text-sm"><thead><tr className="text-right text-ink-400 border-b"><th className="p-3">SKU</th><th className="p-3">معامل التحويل</th><th className="p-3">إجراء</th></tr></thead><tbody>{activeMembers.map(m=><tr key={m.id} className="border-b border-ink-50"><td className="p-3 font-medium">{m.sku}</td><td className="p-3">{m.conversion_factor}</td><td className="p-3"><button onClick={()=>removeMember(m.id)} className="text-red-600 hover:text-red-700"><Trash2 size={16}/></button></td></tr>)}{!activeMembers.length&&<tr><td colSpan={3} className="p-8 text-center text-ink-400">أضف الأصناف التي تعتبر بدائل متكافئة.</td></tr>}</tbody></table></div><div className="rounded-xl bg-ink-50 p-4 text-xs text-ink-500 flex gap-2"><UsersRound size={16}/><span>سيستخدم محرك التقارير هذه المجموعة لتجميع الطلب والمخزون والمبيعات وإعادة حساب التغطية على مستوى المجموعة.</span></div></section>}
 </div>
}
