import { useCallback, useEffect, useMemo, useState } from 'react';
import { AlertTriangle, ArrowLeft, Bell, CheckCircle2, ChevronLeft, CircleDot, FileSearch, Lightbulb, LockKeyhole, ShieldCheck, Target, UserRound, Workflow } from 'lucide-react';
import { fetchAlerts, fetchRecommendations } from '@/lib/queries';
import { formatCurrency, relativeTime } from '@/lib/format';
import type { Alert, Recommendation } from '@/lib/types';

type Stage = 'command' | 'evidence' | 'decision' | 'approval' | 'work' | 'outcome';
type StageMeta = { id: Stage; label: string; description: string; icon: typeof Workflow };
const stages: StageMeta[] = [
  { id: 'command', label: 'مركز القيادة', description: 'صورة العمل والأولويات', icon: Workflow },
  { id: 'evidence', label: 'الدليل', description: 'ما نعرفه ولماذا', icon: FileSearch },
  { id: 'decision', label: 'القرار', description: 'التوصية ومسار القرار', icon: CircleDot },
  { id: 'approval', label: 'الموافقة', description: 'المسؤولية والاعتماد', icon: ShieldCheck },
  { id: 'work', label: 'التنفيذ', description: 'العمل والمتابعة', icon: Target },
  { id: 'outcome', label: 'النتيجة والتعلّم', description: 'Expected → Actual', icon: Lightbulb },
];

function RuntimeBlocked({ title, detail = 'Runtime unavailable — awaiting authenticated environment.' }: { title: string; detail?: string }) {
  return <div role="status" aria-live="polite" className="rounded-2xl border border-amber-200 bg-amber-50 p-4"><div className="flex items-start gap-3"><LockKeyhole size={19} className="mt-0.5 shrink-0 text-amber-700" /><div><p className="text-sm font-semibold text-amber-950">{title}</p><p className="mt-1 text-xs leading-6 text-amber-800">{detail}</p></div></div></div>;
}

function EmptyState({ title, detail }: { title: string; detail: string }) {
  return <div className="rounded-2xl border border-dashed border-ink-300 bg-ink-50/70 p-6 text-center"><div className="mx-auto flex h-10 w-10 items-center justify-center rounded-full bg-white text-ink-500 shadow-sm"><CircleDot size={18} /></div><p className="mt-3 text-sm font-semibold text-ink-800">{title}</p><p className="mt-1 text-xs leading-6 text-ink-500">{detail}</p></div>;
}

function StatePill({ label, tone = 'neutral' }: { label: string; tone?: 'neutral' | 'warning' | 'success' }) {
  const classes = tone === 'warning' ? 'bg-amber-100 text-amber-800' : tone === 'success' ? 'bg-emerald-100 text-emerald-800' : 'bg-ink-100 text-ink-700';
  return <span className={`inline-flex items-center rounded-full px-2.5 py-1 text-[11px] font-semibold ${classes}`}>{label}</span>;
}

export function DecisionExperiencePage() {
  const [stage, setStage] = useState<Stage>('command');
  const [recommendations, setRecommendations] = useState<Recommendation[]>([]);
  const [alerts, setAlerts] = useState<Alert[]>([]);
  const [selectedId, setSelectedId] = useState<string | null>(null);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState<string | null>(null);

  const load = useCallback(async () => {
    setLoading(true); setError(null);
    try {
      const [nextRecommendations, nextAlerts] = await Promise.all([fetchRecommendations(), fetchAlerts()]);
      setRecommendations(nextRecommendations); setAlerts(nextAlerts);
      setSelectedId(current => current && nextRecommendations.some(item => item.id === current) ? current : nextRecommendations[0]?.id ?? null);
    } catch (cause) {
      setError(cause instanceof Error ? cause.message : 'تعذر تحميل بيانات تجربة القرار.');
    } finally { setLoading(false); }
  }, []);
  useEffect(() => { void load(); }, [load]);

  const selected = useMemo(() => recommendations.find(item => item.id === selectedId) ?? null, [recommendations, selectedId]);
  const go = (next: Stage) => setStage(next);

  return <div dir="rtl" className="space-y-5 pb-8">
    <header className="overflow-hidden rounded-3xl bg-ink-950 p-6 text-white shadow-sm lg:p-8">
      <div className="flex flex-col gap-5 xl:flex-row xl:items-end xl:justify-between">
        <div className="max-w-3xl">
          <div className="flex items-center gap-2 text-sm font-semibold text-primary-300"><Workflow size={17} /> منظومة القرار التنفيذية</div>
          <h1 className="mt-2 text-2xl font-bold tracking-tight lg:text-3xl">من البيانات إلى الدليل، ثم القرار والنتيجة</h1>
          <p className="mt-3 text-sm leading-7 text-ink-300">تجربة واحدة لدورة القرار. كل قيمة معروضة تأتي من عقد canonical أو تُعلن صراحةً كغير متاحة؛ لا توجد نجاحات أو أدلة وهمية.</p>
        </div>
        <div className="rounded-2xl border border-white/10 bg-white/10 px-4 py-3 text-xs text-ink-200"><span className="font-semibold text-white">Runtime</span><br />Auth/Tenant authority مطلوبة لأي mutation حقيقي.</div>
      </div>
    </header>

    <nav aria-label="مراحل دورة القرار" className="grid grid-cols-2 gap-2 sm:grid-cols-3 xl:grid-cols-6">
      {stages.map(({ id, label, description, icon: Icon }, index) => <button key={id} type="button" aria-current={stage === id ? 'step' : undefined} onClick={() => go(id)} className={`group rounded-2xl border p-3 text-right transition focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary-500 focus-visible:ring-offset-2 ${stage === id ? 'border-primary-500 bg-primary-50 text-primary-900 shadow-sm' : 'border-ink-200 bg-white text-ink-700 hover:border-ink-300 hover:bg-ink-50'}`}><div className="flex items-center justify-between"><Icon size={18} /><span className="text-[10px] text-ink-400">{index + 1}/6</span></div><span className="mt-2 block text-xs font-bold">{label}</span><span className="mt-1 block text-[11px] text-ink-500">{description}</span></button>)}
    </nav>

    {error && <div role="alert" className="rounded-2xl border border-red-200 bg-red-50 p-4 text-sm text-red-800"><div className="flex items-center gap-2 font-semibold"><AlertTriangle size={18} /> تعذر تحميل البيانات</div><p className="mt-1 text-xs leading-6">{error}</p><button type="button" onClick={() => void load()} className="mt-3 rounded-xl border border-red-300 bg-white px-3 py-2 text-xs font-semibold focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-red-500">إعادة المحاولة</button></div>}

    {stage === 'command' && <section aria-labelledby="command-title" className="space-y-4">
      <div className="rounded-2xl border border-ink-200 bg-white p-5"><div className="flex items-start justify-between gap-4"><div><h2 id="command-title" className="text-lg font-bold text-ink-950">Business Health</h2><p className="mt-1 text-xs text-ink-500">الأولوية هي لما يحتاج قرارًا، لا لكثرة البطاقات.</p></div><StatePill label={loading ? 'جارٍ التحميل' : 'بيانات مصدرية'} tone={loading ? 'neutral' : 'success'} /></div><div className="mt-5 grid grid-cols-2 gap-3 lg:grid-cols-4"><div className="rounded-xl bg-ink-50 p-4"><p className="text-xs text-ink-500">التوصيات</p><p className="mt-1 text-2xl font-bold">{loading ? '—' : recommendations.length}</p></div><div className="rounded-xl bg-ink-50 p-4"><p className="text-xs text-ink-500">التنبيهات</p><p className="mt-1 text-2xl font-bold">{loading ? '—' : alerts.length}</p></div><div className="rounded-xl bg-ink-50 p-4"><p className="text-xs text-ink-500">Pending Decisions</p><p className="mt-1 text-2xl font-bold">{loading ? '—' : recommendations.filter(r => r.status === 'new').length}</p></div><div className="rounded-xl bg-ink-50 p-4"><p className="text-xs text-ink-500">Outcomes</p><p className="mt-1 text-2xl font-bold">{loading ? '—' : recommendations.filter(r => r.status === 'done').length}</p></div></div></div>
      <div className="grid gap-4 lg:grid-cols-[1.45fr_1fr]">
        <div className="rounded-2xl border border-ink-200 bg-white p-5"><div className="flex items-center justify-between"><div><h2 className="font-bold">Critical Insights & Recommendations</h2><p className="mt-1 text-xs text-ink-500">النقر على أي عنصر يبدأ رحلة الدليل.</p></div><Lightbulb size={20} /></div><div className="mt-4 space-y-2">{!loading && recommendations.length === 0 && <EmptyState title="No recommendations yet" detail="ستظهر هنا التوصيات التي يرسلها المصدر المعتمد؛ لم يتم إنشاء بيانات تجريبية." />}{recommendations.slice(0, 8).map(rec => <button key={rec.id} type="button" onClick={() => { setSelectedId(rec.id); go('evidence'); }} className="flex w-full items-center gap-3 rounded-xl border border-ink-100 p-3 text-right transition hover:bg-ink-50 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary-500"><span className="rounded-lg bg-primary-50 p-2 text-primary-600"><Lightbulb size={16} /></span><span className="min-w-0 flex-1"><span className="block truncate text-sm font-semibold">{rec.title}</span><span className="mt-1 block text-xs text-ink-500">{rec.expected_impact ? `الأثر المتوقع: ${formatCurrency(rec.expected_impact)}` : 'الأثر المتوقع غير متاح'}</span></span><ChevronLeft size={17} className="shrink-0 text-ink-400" /></button>)}</div></div>
        <div className="rounded-2xl border border-ink-200 bg-white p-5"><div className="flex items-center gap-2"><Bell size={19} /><h2 className="font-bold">Pending / Alerts</h2></div><div className="mt-4 space-y-2">{!loading && alerts.length === 0 && <EmptyState title="No pending alerts" detail="لا توجد تنبيهات في المصدر الحالي." />}{alerts.slice(0, 6).map(alert => <div key={alert.id} className="rounded-xl border border-ink-100 p-3"><div className="flex items-start justify-between gap-3"><p className="text-sm font-semibold">{alert.title}</p><StatePill label={alert.is_read ? 'Read' : 'Unread'} /></div><p className="mt-1 text-xs text-ink-500">{relativeTime(alert.created_at)}</p></div>)}</div></div>
      </div>
      <div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3"><button type="button" onClick={() => go('decision')} className="rounded-2xl border border-ink-200 bg-white p-5 text-right hover:bg-ink-50 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary-500"><CircleDot size={20} /><p className="mt-3 font-bold">Pending Decisions</p><p className="mt-1 text-xs text-ink-500">انتقل إلى مساحة القرار دون افتراض أن mutation حدثت.</p></button><button type="button" onClick={() => go('approval')} className="rounded-2xl border border-ink-200 bg-white p-5 text-right hover:bg-ink-50 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary-500"><ShieldCheck size={20} /><p className="mt-3 font-bold">Pending Approvals</p><p className="mt-1 text-xs text-ink-500">مراجعة ما سيُعتمد قبل تنفيذ أي action.</p></button><button type="button" onClick={() => go('work')} className="rounded-2xl border border-ink-200 bg-white p-5 text-right hover:bg-ink-50 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary-500"><Target size={20} /><p className="mt-3 font-bold">Active Work</p><p className="mt-1 text-xs text-ink-500">مساحة العمل اليومية مرتبطة بالـruntime.</p></button></div>
    </section>}

    {stage === 'evidence' && <section className="grid gap-4 lg:grid-cols-[0.8fr_1.4fr]">
      <div className="rounded-2xl border border-ink-200 bg-white p-5"><h2 className="font-bold">Insights</h2><div className="mt-4 space-y-2">{recommendations.length === 0 && !loading && <EmptyState title="No recommendations yet" detail="لا يمكن فحص دليل لتوصية غير موجودة." />}{recommendations.map(rec => <button key={rec.id} type="button" aria-pressed={selectedId === rec.id} onClick={() => setSelectedId(rec.id)} className={`w-full rounded-xl border p-3 text-right focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary-500 ${selectedId === rec.id ? 'border-primary-400 bg-primary-50' : 'border-ink-100 hover:bg-ink-50'}`}><p className="text-sm font-semibold">{rec.title}</p><p className="mt-1 text-xs text-ink-500">{rec.status}</p></button>)}</div></div>
      <div className="rounded-2xl border border-ink-200 bg-white p-5"><div className="flex items-center gap-2"><FileSearch size={20} /><h2 className="text-lg font-bold">Evidence Inspector</h2></div>{selected ? <div className="mt-5 space-y-5"><div><p className="text-[11px] font-bold tracking-wide text-ink-400">WHAT / WHY</p><h3 className="mt-1 text-lg font-bold">{selected.title}</h3><p className="mt-2 text-sm leading-7 text-ink-600">{selected.description || 'وصف التوصية غير متاح من المصدر الحالي.'}</p></div><div className="grid gap-3 sm:grid-cols-2"><div className="rounded-xl border border-ink-100 p-4"><p className="text-xs text-ink-500">Expected Impact</p><p className="mt-1 font-bold">{selected.expected_impact ? formatCurrency(selected.expected_impact) : 'Not available'}</p></div><div className="rounded-xl border border-ink-100 p-4"><p className="text-xs text-ink-500">Reliability</p><p className="mt-1 font-bold">Not established from runtime evidence</p></div></div><div className="rounded-xl border border-amber-200 bg-amber-50 p-4"><p className="text-xs font-bold text-amber-900">SOURCE / CALCULATION / RAW EVIDENCE</p><p className="mt-1 text-xs leading-6 text-amber-800">Evidence unavailable until the canonical evidence payload is present. No source, metric calculation, verification, or raw evidence is invented.</p></div><div className="rounded-xl border border-ink-100 p-4"><p className="text-xs font-bold text-ink-700">BUSINESS IMPLICATION</p><p className="mt-1 text-sm text-ink-600">{selected.expected_impact ? 'الأثر المتوقع متاح؛ القرار النهائي يتطلب evidence وruntime authority.' : 'لا يمكن تحديد الأثر التجاري من البيانات الحالية.'}</p></div><button type="button" onClick={() => go('decision')} className="inline-flex items-center gap-2 rounded-xl bg-ink-950 px-4 py-2.5 text-xs font-bold text-white focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary-500">متابعة إلى القرار <ArrowLeft size={14} /></button></div> : <EmptyState title="Select an insight" detail="اختر توصية موجودة لفحص ما هو متاح من مصدرها canonical." />}</div>
    </section>}

    {stage === 'decision' && <section className="space-y-4"><div className="rounded-2xl border border-ink-200 bg-white p-5"><div className="flex flex-wrap items-center justify-between gap-3"><div><p className="text-xs text-ink-500">Decision Workspace</p><h2 className="mt-1 text-lg font-bold">Recommendation → Impact → Evidence → Decision</h2></div><StatePill label="PENDING_APPROVAL" tone="warning" /></div>{selected ? <div className="mt-5 grid gap-4 sm:grid-cols-2 lg:grid-cols-4"><div><p className="text-xs text-ink-500">Recommendation</p><p className="mt-1 text-sm font-semibold">{selected.title}</p></div><div><p className="text-xs text-ink-500">Expected Impact</p><p className="mt-1 text-sm font-semibold">{selected.expected_impact ? formatCurrency(selected.expected_impact) : 'غير متاح'}</p></div><div><p className="text-xs text-ink-500">Decision</p><p className="mt-1 text-sm font-semibold">لم يُنفذ</p></div><div><p className="text-xs text-ink-500">Owner</p><p className="mt-1 text-sm font-semibold">غير متاح</p></div></div> : <EmptyState title="No decision candidate" detail="لا يوجد قرار يمكن ربطه بتوصية حالية." />}</div><div className="grid gap-3 md:grid-cols-2">{['PROPOSED','PENDING_APPROVAL','APPROVED / REJECTED','OPEN','IN_PROGRESS','COMPLETED','OUTCOME'].map(state => <div key={state} className="rounded-xl border border-ink-100 bg-white p-4"><p className="text-xs font-bold">{state}</p><p className="mt-1 text-xs text-ink-500">الحالة لا تُدّعى؛ تنتقل فقط عبر canonical lifecycle mutation عند توفر runtime.</p></div>)}</div><RuntimeBlocked title="Decision actions unavailable" /><div className="flex justify-end"><button type="button" onClick={() => go('approval')} className="inline-flex items-center gap-2 rounded-xl border border-ink-300 bg-white px-4 py-2.5 text-xs font-bold focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary-500">عرض الموافقة <ArrowLeft size={14} /></button></div></section>}

    {stage === 'approval' && <section className="space-y-4"><div className="rounded-2xl border border-ink-200 bg-white p-5"><div className="flex items-center gap-2"><ShieldCheck size={20} /><h2 className="text-lg font-bold">Approval Center</h2></div><p className="mt-2 text-sm leading-7 text-ink-600">قبل الاعتماد: ما الذي أوافق عليه؟ لماذا؟ ما الدليل؟ ما الأثر المتوقع؟ من المسؤول؟ وما الخطوة التالية؟</p><div className="mt-5 grid gap-3 md:grid-cols-2"><div className="rounded-xl bg-ink-50 p-4"><p className="text-xs text-ink-500">What / Why</p><p className="mt-1 text-sm font-semibold">{selected?.title ?? 'لا يوجد قرار محدد'}</p></div><div className="rounded-xl bg-ink-50 p-4"><p className="text-xs text-ink-500">Expected Impact</p><p className="mt-1 text-sm font-semibold">{selected?.expected_impact ? formatCurrency(selected.expected_impact) : 'غير متاح'}</p></div><div className="rounded-xl bg-ink-50 p-4"><p className="text-xs text-ink-500">Evidence</p><p className="mt-1 text-sm font-semibold">Unavailable / runtime evidence required</p></div><div className="rounded-xl bg-ink-50 p-4"><p className="text-xs text-ink-500">Accountability / Next</p><p className="mt-1 text-sm font-semibold">Owner and next action unavailable</p></div></div></div><RuntimeBlocked title="Approval action unavailable" detail="Authenticated runtime required. لا يتم إنشاء approval أو timestamp أو approver محليًا." /><div className="grid gap-3 sm:grid-cols-2"><button type="button" onClick={() => go('evidence')} className="rounded-xl border border-ink-300 bg-white px-4 py-3 text-xs font-bold focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary-500">عودة إلى الدليل</button><button type="button" onClick={() => go('work')} className="rounded-xl border border-ink-300 bg-white px-4 py-3 text-xs font-bold focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary-500">مساحة العمل</button></div></section>}

    {stage === 'work' && <section className="space-y-4"><div className="rounded-2xl border border-ink-200 bg-white p-5"><div className="flex items-center gap-2"><Target size={20} /><h2 className="text-lg font-bold">Personal Workbench</h2></div><p className="mt-2 text-sm text-ink-500">الأولوية للعمل المعيّن والمتأخر، وليس لجدول فارغ.</p><div className="mt-5 grid grid-cols-2 gap-3 md:grid-cols-3">{[['MY APPROVALS','الموافقات التي تحتاجك'],['MY TASKS','المهام المعيّنة لك'],['IN PROGRESS','العمل الجاري'],['OVERDUE','ما تجاوز موعده'],['COMPLETED','العمل المكتمل'],['OUTCOME FOLLOW-UP','متابعة النتائج']].map(([label, detail]) => <div key={label} className="rounded-xl border border-ink-100 p-4"><p className="text-xs font-bold">{label}</p><p className="mt-2 text-xs leading-5 text-ink-500">{detail}</p><p className="mt-3 text-xs font-semibold text-ink-700">لا توجد بيانات runtime متاحة</p></div>)}</div></div><RuntimeBlocked title="No active work" detail="لا يمكن اختلاق مهام أو إسناد أو حالات إنجاز. Authenticated runtime required." /><EmptyState title="You're all caught up." detail="No assigned work items yet." /></section>}

    {stage === 'outcome' && <section className="space-y-4"><div className="rounded-2xl border border-ink-200 bg-white p-5"><div className="flex items-center gap-2"><Lightbulb size={20} /><h2 className="text-lg font-bold">Outcome & Learning</h2></div><p className="mt-2 text-sm leading-7 text-ink-600">النتيجة تقارن ما كان متوقعًا بما حدث فعليًا، ثم تقيس الانحراف وجودة النتيجة وتجمع feedback قبل أي learning signal.</p><div className="mt-5 grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{[['EXPECTED','الأثر المتوقع من التوصية'],['ACTUAL','النتيجة الفعلية'],['DELTA','Actual − Expected'],['QUALITY','جودة النتيجة'],['FEEDBACK','ملاحظات التنفيذ'],['LEARNING SIGNAL','إشارة تعلّم قابلة للإثبات']].map(([label, detail]) => <div key={label} className="rounded-xl border border-ink-100 p-4"><p className="text-xs font-bold">{label}</p><p className="mt-2 text-xs text-ink-500">{detail}</p><p className="mt-3 text-sm font-semibold">Awaiting actual outcome</p></div>)}</div></div><RuntimeBlocked title="Learning is not certified" detail="Repository Learning ≠ Runtime Learning. لا يوجد actual outcome أو feedback أو learning evidence runtime في البيئة الحالية." /></section>}
  </div>;
}
