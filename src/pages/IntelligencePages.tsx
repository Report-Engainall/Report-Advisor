/**
 * @deprecated Compatibility surface only.
 * The canonical intelligence page implementations live in `IntelligencePage.tsx`.
 * Keep this module as a zero-logic re-export so legacy imports do not create a second implementation.
 */
export { RecommendationsPage, ForecastsPage } from './IntelligencePage';
