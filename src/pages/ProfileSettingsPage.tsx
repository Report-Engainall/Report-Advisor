import { useEffect,useState } from 'react';
import type { User } from '@supabase/supabase-js';
import { CheckCircle2,Save,UserCircle,ShieldCheck,Sparkles } from 'lucide-react';
import { Card,CardBody,CardHeader } from '@/components/ui/Card';
import { supabase } from '@/lib/supabase';
export function ProfileSettingsPage(){const[user,setUser]=useState<User|null>(null);const[displayName,setDisplayName]=useState('');const[loading,setLoading]=useState(true);const[saving,setSaving]=useState(false);const[message,setMessage]=useState<string|null>(null);const[error,setError]=useState<string|null>(null);useEffect(()=>{let mounted=true;void supabase.auth.getUser().then(({data,error:userError})=>{if(!mounted)return;if(userError)setError(userError.message);const currentUser=data.user??null;setUser(currentUser);setDisplayName(typeof currentUser?.user_metadata?.full_name==='string'?currentUser.user_metadata.full_name:'');setLoading(false)});return()=>{mounted=false}},[]);const save=async()=>{if(!user)return;setSaving(true);setMessage(null);setError(null);const cleanName=displayName.trim();const{data,error:updateError}=await supabase.auth.updateUser({data:{full_name:cleanName||null}});setSaving(false);if(updateError){setError(updateError.message);return}if(data.user)setUser(data.user);setMessage('تم حفظ اسم العرض بنجاح. سيظهر الاسم الجديد في واجهة النظام.')};if(loading)return <div dir="rtl" className="py-20 text-center text-sm text-ink-500">جارٍ تحميل بيانات الحساب...</div>;return <div dir="rtl" className="space-y-7 pb-10 animate-fade-in"><section className="experience-hero"><div className="relative z-10"><div className="experience-chip"><Sparkles size={14}/> Personal Workspace</div><h1 className="mt-4 text-3xl font-black tracking-tight sm:text-4xl lg:text-5xl">ملفك الشخصي</h1><p className="mt-3 max-w-2xl text-sm leading-7 text-white/70">هوية المستخدم التي تظهر داخل مساحة العمل، مع بقاء البريد الإلكتروني تحت إدارة نظام المصادقة.</p></div></section><Card className="overflow-hidden rounded-[1.75rem] border-0 shadow-elevated"><CardHeader title="هوية المستخدم" subtitle="بيانات الحساب من Supabase Auth"/><CardBody><div className="flex flex-col gap-6 sm:flex-row sm:items-center"><div className="flex h-20 w-20 shrink-0 items-center justify-center rounded-[1.5rem] bg-gradient-to-br from-emerald-100 to-amber-100 text-emerald-800 ring-1 ring-emerald-100"><UserCircle size={44}/></div><div><div className="text-lg font-black text-ink-900">{displayName||'مستخدم النظام'}</div><div className="mt-1 text-sm text-ink-500">{user?.email||'لم يتم تحديد البريد الإلكتروني'}</div><div className="mt-2 inline-flex items-center gap-1.5 rounded-full bg-emerald-50 px-2.5 py-1 text-[11px] font-bold text-emerald-700"><ShieldCheck size={13}/> حساب موثّق من مصدر المصادقة</div></div></div><div className="mt-8 max-w-2xl"><label htmlFor="profile-display-name" className="mb-2 block text-sm font-bold text-ink-700">اسم العرض</label><input id="profile-display-name" value={displayName} onChange={event=>setDisplayName(event.target.value)} maxLength={120} placeholder="اكتب الاسم الذي تريد ظهوره في النظام" className="input h-12 w-full rounded-2xl" autoComplete="name"/><p className="mt-2 text-xs text-ink-400">يمكن تغييره لاحقًا دون تعديل الكود.</p><div className="mt-5 flex flex-wrap items-center gap-3"><button type="button" disabled={saving||!user} onClick={()=>void save()} className="btn-primary inline-flex items-center gap-2 disabled:opacity-50"><Save size={16}/>{saving?'جارٍ الحفظ...':'حفظ اسم العرض'}</button>{message&&<span className="inline-flex items-center gap-1.5 text-sm text-success-600"><CheckCircle2 size={16}/>{message}</span>}{error&&<span role="alert" className="text-sm text-danger-600">{error}</span>}</div></div></CardBody></Card></div>}
