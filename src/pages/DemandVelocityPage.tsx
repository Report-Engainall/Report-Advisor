import { useEffect,useMemo,useState } from 'react';
import { Activity, TrendingUp, TrendingDown, Minus, RefreshCw } from 'lucide-react';
import { Link } from 'react-router-dom';
import { Card,CardHeader,CardBody } from '@/components/ui/Card';
import { Badge } from '@/components/ui/Badge';
import { PageHeader,LoadingState,ErrorState,EmptyState } from '@/components/ui/States';
import { fetchProductDemandSeries, type ProductDemandSeries } from '@/lib/free-toolbox/sales-demand-series';
import { formatNumber } from '@/lib/format';
export function DemandVelocityPage(){const[data,setData]=useState<ProductDemandSeries[]>([]);const[days,setDays]=useState(180);const[loading,setLoading]=useState(true);const[error,setError]=useState<string|null>(null);const load=async()=>{setLoading(true);setError(null);try{setData(await fetchProductDemandSeries(days))}catch(e:unknown){setError(e instanceof Error?e.message:'تعذر تحميل حركة الطلب')}finally{setLoading(false)}};useEffect(()=>{load()},[days]);const summary=useMemo(()=>({items:data.length,units:data.reduce((s,x)=>s+x.totalQuantity,0),accelerating:data.filter(x=>x.trend>.2).length,declining:data.filter(x=>x.trend<-.2).length}),[data]); const nextAction=!data.length?{to:'/import',label:'إدخال مصدر'}:summary.declining>0?{to:'/reports/sales',label:'مراجعة المبيعات'}:{to:'/trust',label:'فحص الدليل'};if(loading)return <LoadingState message="جارٍ تحليل حركة الطلب التاريخية..."/>;if(error)return <ErrorState message={error} onRetry={load}/>;return <div dir="rtl" className="ag-ops-intelligence ag-analytics-surface space-y-5 animate-fade-in"><PageHeader title="حركة الطلب وسرعة الأصناف" subtitle="تحليل فعلي للمبيعات التاريخية لاكتشاف التسارع والتراجع والذروة" actions={<button type="button" onClick={load} className="btn-secondary"><RefreshCw size={16}/> تحديث</button>} /><div className="rounded-2xl border border-ink-100 bg-white px-4 py-3 shadow-sm" aria-label="سياق حقيقة حركة الطلب"><div className="flex flex-col gap-2 sm:flex-row sm:items-center sm:justify-between"><div><div className="text-[10px] font-black uppercase tracking-[0.14em] text-primary-700">TRUTH CONTEXT</div><p className="mt-1 text-xs font-semibold text-ink-800">المصدر: فواتير المبيعات وبنودها ضمن نافذة {days} يومًا للشركة الحالية.</p><p className="mt-1 text-[11px] text-ink-500">المعادلات حتمية من السجلات المتاحة، ولا تُستبدل القيم الناقصة بتقديرات مصطنعة.</p></div><Link to="/trust" className="inline-flex min-h-11 items-center justify-center rounded-xl border border-ink-200 px-3 text-xs font-bold text-ink-700 hover:bg-ink-50">فحص الدليل</Link></div></div><div className="filter-toolbar w-fit" role="toolbar" aria-label="الفترة الزمنية">{[30,90,180,365].map(n=><button type="button" key={n} aria-pressed={days===n} onClick={()=>setDays(n)} className={`filter-chip ${days===n?'filter-chip-active':'hover:bg-white'}`}>{n} يوم</button>)}</div><div className="grid grid-cols-2 lg:grid-cols-4 gap-4"><Card><CardBody><div className="text-xs text-ink-500">الأصناف ذات الحركة</div><div className="display-number mt-1">{formatNumber(summary.items)}</div></CardBody></Card><Card><CardBody><div className="text-xs text-ink-500">إجمالي الوحدات</div><div className="display-number mt-1">{formatNumber(summary.units)}</div></CardBody></Card><Card><CardBody><div className="text-xs text-ink-500">طلب متسارع</div><div className="display-number mt-1 text-success-700">{summary.accelerating}</div></CardBody></Card><Card><CardBody><div className="text-xs text-ink-500">طلب متراجع</div><div className="display-number mt-1 text-danger-700">{summary.declining}</div></CardBody></Card></div><Card><CardHeader title="الأصناف الأعلى حركة" subtitle="الترتيب حسب الكمية المباعة خلال الفترة"/><CardBody><div className="overflow-x-auto"><table className="w-full text-sm"><thead><tr className="text-right text-xs text-ink-400 border-b"><th className="py-3">الصنف</th><th>SKU</th><th>إجمالي الوحدات</th><th>المتوسط اليومي</th><th>الذروة اليومية</th><th>الاتجاه</th></tr></thead><tbody>{data.slice(0,50).map(x=>{const up=x.trend>.2,down=x.trend<-.2;return <tr key={x.productId} className="border-b border-ink-50"><td className="py-3 font-medium">{x.name}</td><td className="text-ink-500">{x.sku||'—'}</td><td>{formatNumber(x.totalQuantity)}</td><td>{x.averageDaily.toFixed(2)}</td><td>{formatNumber(x.peakDaily)}</td><td><Badge variant={up?'success':down?'danger':'neutral'}>{up?<TrendingUp size={13}/>:down?<TrendingDown size={13}/>:<Minus size={13}/>} {`${(x.trend*100).toFixed(1)}%`}</Badge></td></tr>})}</tbody></table>{data.length===0&&<EmptyState icon={<Activity size={24} className="text-ink-300"/>} title="لا توجد مبيعات ضمن الفترة المحددة" message="لا يمكن اشتقاق سرعة الطلب من دون سجلات مبيعات للفترة المختارة." action={<Link to={nextAction.to} className="btn-primary min-h-11">{nextAction.label}</Link>}/>}</div></CardBody></Card></div>}
