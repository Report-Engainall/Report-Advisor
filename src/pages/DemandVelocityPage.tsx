import { useEffect,useMemo,useState } from 'react';
import { Activity, TrendingUp, TrendingDown, Minus, RefreshCw } from 'lucide-react';
import { Card,CardHeader,CardBody } from '@/components/ui/Card';
import { Badge } from '@/components/ui/Badge';
import { LoadingState,ErrorState } from '@/components/ui/States';
import { fetchProductDemandSeries, type ProductDemandSeries } from '@/lib/free-toolbox/sales-demand-series';
import { formatNumber } from '@/lib/format';
export function DemandVelocityPage(){const[data,setData]=useState<ProductDemandSeries[]>([]);const[days,setDays]=useState(180);const[loading,setLoading]=useState(true);const[error,setError]=useState<string|null>(null);const load=async()=>{setLoading(true);setError(null);try{setData(await fetchProductDemandSeries(days))}catch(e:any){setError(e?.message||'تعذر تحميل حركة الطلب')}finally{setLoading(false)}};useEffect(()=>{load()},[days]);const summary=useMemo(()=>({items:data.length,units:data.reduce((s,x)=>s+x.totalQuantity,0),accelerating:data.filter(x=>x.trend>.2).length,declining:data.filter(x=>x.trend<-.2).length}),[data]);if(loading)return <LoadingState message="جارٍ تحليل حركة الطلب التاريخية..."/>;if(error)return <ErrorState message={error} onRetry={load}/>;return <div className="space-y-6 animate-fade-in"><div className="flex items-start justify-between gap-4"><div><h1 className="text-2xl font-bold text-ink-900">حركة الطلب وسرعة الأصناف</h1><p className="text-sm text-ink-500 mt-1">تحليل فعلي للمبيعات التاريخية لاكتشاف التسارع والتراجع والذروة</p></div><button onClick={load} className="inline-flex items-center gap-2 px-3 py-2 rounded-lg border border-ink-200 bg-white text-sm"><RefreshCw size={16}/> تحديث</button></div><div className="flex gap-2">{[30,90,180,365].map(n=><button key={n} onClick={()=>setDays(n)} className={`px-3 py-1.5 rounded-lg text-xs ${days===n?'bg-primary-600 text-white':'bg-ink-100 text-ink-600'}`}>{n} يوم</button>)}</div><div className="grid grid-cols-2 lg:grid-cols-4 gap-4"><Card><CardBody><div className="text-xs text-ink-500">الأصناف ذات الحركة</div><div className="text-2xl font-bold mt-1">{formatNumber(summary.items)}</div></CardBody></Card><Card><CardBody><div className="text-xs text-ink-500">إجمالي الوحدات</div><div className="text-2xl font-bold mt-1">{formatNumber(summary.units)}</div></CardBody></Card><Card><CardBody><div className="text-xs text-ink-500">طلب متسارع</div><div className="text-2xl font-bold text-success-600 mt-1">{summary.accelerating}</div></CardBody></Card><Card><CardBody><div className="text-xs text-ink-500">طلب متراجع</div><div className="text-2xl font-bold text-danger-600 mt-1">{summary.declining}</div></CardBody></Card></div><Card><CardHeader title="الأصناف الأعلى حركة" subtitle="الترتيب حسب الكمية المباعة خلال الفترة"/><CardBody><div className="overflow-x-auto"><table className="w-full text-sm"><thead><tr className="text-right text-xs text-ink-400 border-b"><th className="py-3">الصنف</th><th>SKU</th><th>إجمالي الوحدات</th><th>المتوسط اليومي</th><th>الذروة اليومية</th><th>الاتجاه</th></tr></thead><tbody>{data.slice(0,50).map(x=>{const up=x.trend>.2,down=x.trend<-.2;return <tr key={x.productId} className="border-b border-ink-50"><td className="py-3 font-medium">{x.name}</td><td className="text-ink-500">{x.sku||'—'}</td><td>{formatNumber(x.totalQuantity)}</td><td>{x.averageDaily.toFixed(2)}</td><td>{formatNumber(x.peakDaily)}</td><td><Badge variant={up?'success':down?'danger':'default'}>{up?<TrendingUp size={13}/>:down?<TrendingDown size={13}/>:<Minus size={13}/>} {`${(x.trend*100).toFixed(1)}%`}</Badge></td></tr>})}</tbody></table>{data.length===0&&<div className="py-10 text-center text-sm text-ink-400"><Activity className="mx-auto mb-2" size={24}/>لا توجد مبيعات ضمن الفترة المحددة</div>}</div></CardBody></Card></div>}
