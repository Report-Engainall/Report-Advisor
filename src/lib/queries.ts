import { supabase, resolveCurrentCompanyId } from './supabase';
import type { Recommendation, Alert, SalesInvoice, PurchaseInvoice, InventoryBalance, ImportRecord, Customer, Forecast, Product } from './types';
interface InvoiceRow { id: string; total: number | null; paid_amount: number | null; subtotal: number | null; tax_amount: number | null; status: string | null; invoice_date: string; due_date: string | null; }
interface SaleItemRow { invoice_id?: string; line_total: number | null; cost_price: number | null; quantity: number | null; product_id?: string | null; product?: { name: string | null; category_id: string | null } | Array<{ name: string | null; category_id: string | null }> | null; }
interface BalanceRow { quantity: number | null; unit_cost: number | null; }
interface PurchaseRow { total: number | null; paid_amount: number | null; }
interface CustomerAggregateRow { customer_id: string; subtotal: number | null; customer?: { name: string | null } | Array<{ name: string | null }> | null; }
interface ProductAggregateRow { product_id: string | null; quantity: number | null; line_total: number | null; product?: { name: string | null } | Array<{ name: string | null }> | null; }
interface CategoryRow { id: string; name: string; }
function requiredNumber(value: number | null | undefined, field: string): number { if (value === null || value === undefined || !Number.isFinite(value)) throw new Error(`REPORT_DATA_UNAVAILABLE: required numeric field '${field}' is missing or invalid`); return value; }
function validDate(value: string | null | undefined, field: string): Date { if (!value) throw new Error(`REPORT_DATA_UNAVAILABLE: required date field '${field}' is missing`); const date = new Date(value); if (Number.isNaN(date.getTime())) throw new Error(`REPORT_DATA_UNAVAILABLE: required date field '${field}' is invalid`); return date; }
export interface DashboardKPIs { totalSales:number|null;totalCost:number|null;grossProfit:number|null;grossMargin:number|null;totalReceivables:number|null;overdueReceivables:number|null;totalPayables:number|null;inventoryValue:number|null;totalCustomers:number;activeCustomers:number|null;totalProducts:number;invoiceCount:number;avgInvoiceValue:number|null;collectionRate:number|null;status:'CONFIRMED'|'CALCULATED'|'INSUFFICIENT_DATA'; }
export interface MonthlyTrend {month:string;label:string;sales:number;cost:number;profit:number;invoices:number;} export interface TopEntity{id:string;name:string;value:number;secondary?:number;} export interface AgingBucket{bucket:string;amount:number;count:number;} export interface CategoryBreakdown{name:string;sales:number;profit:number;quantity:number;}
export async function fetchDashboardKPIs():Promise<DashboardKPIs>{const {data:invoices,error:invoicesError}=await supabase.from('sales_invoices').select('id,total,paid_amount,subtotal,tax_amount,status,invoice_date,due_date');if(invoicesError)throw invoicesError;const invArr=(invoices??[]) as InvoiceRow[];const invoiceIds=invArr.map(i=>i.id);const {data:items,error:itemsError}=invoiceIds.length?await supabase.from('sale_items').select('invoice_id,line_total,cost_price,quantity').in('invoice_id',invoiceIds):{data:[],error:null};if(itemsError)throw itemsError;const itemRows=(items??[]) as SaleItemRow[];const {data:balances,error:balancesError}=await supabase.from('inventory_balances').select('quantity,unit_cost');if(balancesError)throw balancesError;const balanceRows=(balances??[]) as BalanceRow[];const {count:customerCount,error:customerError}=await supabase.from('customers').select('id',{count:'exact',head:true});if(customerError)throw customerError;const {count:productCount,error:productError}=await supabase.from('products').select('id',{count:'exact',head:true});if(productError)throw productError;const {data:purchases,error:purchasesError}=await supabase.from('purchase_invoices').select('total,paid_amount');if(purchasesError)throw purchasesError;const purchaseRows=(purchases??[]) as PurchaseRow[];const totalCustomers=customerCount??0,totalProducts=productCount??0,activeCustomers:number|null=null;const hasTransactionalData=invArr.length>0||purchaseRows.length>0||balanceRows.length>0;if(!hasTransactionalData)return{totalSales:null,totalCost:null,grossProfit:null,grossMargin:null,totalReceivables:null,overdueReceivables:null,totalPayables:null,inventoryValue:null,totalCustomers,activeCustomers,totalProducts,invoiceCount:0,avgInvoiceValue:null,collectionRate:null,status:'INSUFFICIENT_DATA'};const totalSales=invArr.reduce((s,i)=>s+requiredNumber(i.subtotal,'sales_invoices.subtotal'),0);const totalCost=itemRows.reduce((s,i)=>s+requiredNumber(i.cost_price,'sale_items.cost_price')*requiredNumber(i.quantity,'sale_items.quantity'),0);const grossProfit=totalSales-totalCost,grossMargin=totalSales!==0?(grossProfit/totalSales)*100:null,totalReceivables=invArr.reduce((s,i)=>s+requiredNumber(i.total,'sales_invoices.total')-requiredNumber(i.paid_amount,'sales_invoices.paid_amount'),0);const today=new Date().toISOString().split('T')[0];const overdueReceivables=invArr.filter(i=>i.due_date&&i.due_date<today&&requiredNumber(i.paid_amount,'sales_invoices.paid_amount')<requiredNumber(i.total,'sales_invoices.total')).reduce((s,i)=>s+requiredNumber(i.total,'sales_invoices.total')-requiredNumber(i.paid_amount,'sales_invoices.paid_amount'),0);const totalPayables=purchaseRows.reduce((s,p)=>s+requiredNumber(p.total,'purchase_invoices.total')-requiredNumber(p.paid_amount,'purchase_invoices.paid_amount'),0),inventoryValue=balanceRows.reduce((s,b)=>s+requiredNumber(b.quantity,'inventory_balances.quantity')*requiredNumber(b.unit_cost,'inventory_balances.unit_cost'),0),invoiceCount=invArr.length,avgInvoiceValue=invoiceCount?totalSales/invoiceCount:null,totalPaid=invArr.reduce((s,i)=>s+requiredNumber(i.paid_amount,'sales_invoices.paid_amount'),0),totalInvAmount=invArr.reduce((s,i)=>s+requiredNumber(i.total,'sales_invoices.total'),0),collectionRate=totalInvAmount?(totalPaid/totalInvAmount)*100:null;return{totalSales,totalCost,grossProfit,grossMargin,totalReceivables,overdueReceivables,totalPayables,inventoryValue,totalCustomers,activeCustomers,totalProducts,invoiceCount,avgInvoiceValue,collectionRate,status:'CALCULATED'};}
export async function fetchMonthlyTrend(months=6):Promise<MonthlyTrend[]>{const {data:invoices,error}=await supabase.from('sales_invoices').select('id,subtotal,invoice_date').order('invoice_date',{ascending:true});if(error)throw error;const rows=(invoices??[]) as Array<Pick<InvoiceRow,'id'|'subtotal'|'invoice_date'>>;if(!rows.length)return[];const ids=rows.map(x=>x.id);const {data:items,error:ie}=await supabase.from('sale_items').select('invoice_id,line_total,cost_price,quantity').in('invoice_id',ids);if(ie)throw ie;const cost=new Map<string,number>();for(const i of(items??[]) as SaleItemRow[]){if(!i.invoice_id)continue;const v=requiredNumber(i.cost_price,'sale_items.cost_price')*requiredNumber(i.quantity,'sale_items.quantity');cost.set(i.invoice_id,(cost.get(i.invoice_id)??0)+v);}const by=new Map<string,{sales:number;cost:number;invoices:number}>();for(const inv of rows){const d=validDate(inv.invoice_date,'sales_invoices.invoice_date'),key=`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}`,e=by.get(key)??{sales:0,cost:0,invoices:0};e.sales+=requiredNumber(inv.subtotal,'sales_invoices.subtotal');e.cost+=cost.get(inv.id)??0;e.invoices++;by.set(key,e);}const labels=['يناير','فبراير','مارس','أبريل','مايو','يونيو','يوليو','أغسطس','سبتمبر','أكتوبر','نوفمبر','ديسمبر'],out:MonthlyTrend[]=[],now=new Date();for(let i=months-1;i>=0;i--){const d=new Date(now.getFullYear(),now.getMonth()-i,1),key=`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}`,e=by.get(key)??{sales:0,cost:0,invoices:0};out.push({month:key,label:labels[d.getMonth()],sales:e.sales,cost:e.cost,profit:e.sales-e.cost,invoices:e.invoices});}return out;}
export async function fetchTopCustomers(limit=5):Promise<TopEntity[]>{const {data,error}=await supabase.from('sales_invoices').select('customer_id,subtotal,customer:customers(name)');if(error)throw error;const m=new Map<string,{name:string;value:number}>();for(const r of(data??[]) as CustomerAggregateRow[]){const rel=r.customer,name=Array.isArray(rel)?rel[0]?.name??r.customer_id:rel?.name??r.customer_id,e=m.get(r.customer_id)??{name,value:0};e.value+=requiredNumber(r.subtotal,'sales_invoices.subtotal');m.set(r.customer_id,e);}return[...m.entries()].map(([id,v])=>({id,name:v.name,value:v.value})).sort((a,b)=>b.value-a.value).slice(0,limit);}
export async function fetchTopProducts(limit=5):Promise<TopEntity[]>{const {data:invoices,error:ie}=await supabase.from('sales_invoices').select('id');if(ie)throw ie;const ids=(invoices??[]).map(i=>i.id as string);if(!ids.length)return[];const {data,error}=await supabase.from('sale_items').select('product_id,quantity,line_total,product:products(name)').in('invoice_id',ids).not('product_id','is',null);if(error)throw error;const m=new Map<string,{name:string;value:number;qty:number}>();for(const r of(data??[]) as ProductAggregateRow[]){if(!r.product_id)continue;const rel=r.product,name=Array.isArray(rel)?rel[0]?.name??r.product_id:rel?.name??r.product_id,e=m.get(r.product_id)??{name,value:0,qty:0};e.value+=requiredNumber(r.line_total,'sale_items.line_total');e.qty+=requiredNumber(r.quantity,'sale_items.quantity');m.set(r.product_id,e);}return[...m.entries()].map(([id,v])=>({id,name:v.name,value:v.value,secondary:v.qty})).sort((a,b)=>b.value-a.value).slice(0,limit);}
export async function fetchCategoryBreakdown():Promise<CategoryBreakdown[]>{const {data:invoices,error:ie}=await supabase.from('sales_invoices').select('id');if(ie)throw ie;const ids=(invoices??[]).map(i=>i.id as string);if(!ids.length)return[];const {data:items,error}=await supabase.from('sale_items').select('product_id,quantity,line_total,cost_price,product:products(category_id)').in('invoice_id',ids).not('product_id','is',null);if(error)throw error;const rows=(items??[]) as SaleItemRow[],cats=Array.from(new Set(rows.map(r=>{const p=r.product;return Array.isArray(p)?p[0]?.category_id:p?.category_id;}).filter((x):x is string=>Boolean(x))));const names=new Map<string,string>();if(cats.length){const {data,error:ce}=await supabase.from('categories').select('id,name').in('id',cats);if(ce)throw ce;for(const c of(data??[]) as CategoryRow[])names.set(c.id,c.name);}const m=new Map<string,CategoryBreakdown>();for(const r of rows){const p=r.product,cid=Array.isArray(p)?p[0]?.category_id:p?.category_id,key=cid??'__uncategorized__',e=m.get(key)??{name:cid?(names.get(cid)??cid):'غير مصنف',sales:0,profit:0,quantity:0},sales=requiredNumber(r.line_total,'sale_items.line_total'),cost=requiredNumber(r.cost_price,'sale_items.cost_price')*requiredNumber(r.quantity,'sale_items.quantity');e.sales+=sales;e.profit+=sales-cost;e.quantity+=requiredNumber(r.quantity,'sale_items.quantity');m.set(key,e);}return[...m.values()].sort((a,b)=>b.sales-a.sales);}
export async function fetchRecommendations():Promise<Recommendation[]>{const {data,error}=await supabase.from('recommendations').select('id,company_id,category,priority,title,description,expected_impact,confidence,status,owner,deadline,impact_result,created_at').order('created_at',{ascending:false});if(error)throw error;return(data??[]) as Recommendation[];}
export async function fetchAlerts():Promise<Alert[]>{const {data,error}=await supabase.from('alerts').select('id,company_id,severity,category,title,description,metric_value,threshold,is_read,created_at').order('created_at',{ascending:false});if(error)throw error;return(data??[]) as Alert[];}
export async function fetchAgingBuckets():Promise<AgingBucket[]>{const {data,error}=await supabase.from('sales_invoices').select('total,paid_amount,due_date,invoice_date');if(error)throw error;const buckets:AgingBucket[]=[{bucket:'0-30',amount:0,count:0},{bucket:'31-60',amount:0,count:0},{bucket:'61-90',amount:0,count:0},{bucket:'90+',amount:0,count:0}],today=new Date();for(const inv of(data??[]) as Array<Pick<InvoiceRow,'total'|'paid_amount'|'due_date'|'invoice_date'>>){const outstanding=requiredNumber(inv.total,'sales_invoices.total')-requiredNumber(inv.paid_amount,'sales_invoices.paid_amount');if(outstanding<=0)continue;const due=validDate(inv.due_date,'sales_invoices.due_date'),days=Math.max(0,Math.floor((today.getTime()-due.getTime())/86400000)),idx=days<=30?0:days<=60?1:days<=90?2:3;buckets[idx].amount+=outstanding;buckets[idx].count++;}return buckets;}
export async function fetchSalesInvoices(page=0,pageSize=20):Promise<{data:SalesInvoice[];count:number|null}>{if(!Number.isInteger(page)||page<0)throw new Error('REPORT_QUERY_INVALID_PAGE');if(!Number.isInteger(pageSize)||pageSize<1||pageSize>500)throw new Error('REPORT_QUERY_INVALID_PAGE_SIZE');const from=page*pageSize,to=from+pageSize-1,{data,count,error}=await supabase.from('sales_invoices').select('*, customer:customers(id,name)',{count:'exact'}).order('invoice_date',{ascending:false}).range(from,to);if(error)throw error;return{data:(data??[]) as SalesInvoice[],count};}
export async function fetchPurchaseInvoices(page=0,pageSize=20):Promise<{data:PurchaseInvoice[];count:number|null}>{if(!Number.isInteger(page)||page<0)throw new Error('REPORT_QUERY_INVALID_PAGE');if(!Number.isInteger(pageSize)||pageSize<1||pageSize>500)throw new Error('REPORT_QUERY_INVALID_PAGE_SIZE');const from=page*pageSize,to=from+pageSize-1,{data,count,error}=await supabase.from('purchase_invoices').select('*, supplier:suppliers(id,name)',{count:'exact'}).order('invoice_date',{ascending:false}).range(from,to);if(error)throw error;return{data:(data??[]) as PurchaseInvoice[],count};}
export async function fetchInventoryBalances():Promise<InventoryBalance[]>{const {data,error}=await supabase.from('inventory_balances').select('*, product:products(id,name,reorder_point), warehouse:warehouses(id,name)').order('updated_at',{ascending:false});if(error)throw error;return(data??[]) as InventoryBalance[];}

type ImportRecordInput = Omit<ImportRecord, 'id' | 'company_id' | 'created_at' | 'error_message' | 'completed_at'>;
type ImportRecordPatch = Partial<Pick<ImportRecord, 'status' | 'progress' | 'error_message' | 'completed_at'>>;
export async function createImportRecord(input: ImportRecordInput): Promise<ImportRecord> { const companyId = await resolveCurrentCompanyId(); if (!companyId) throw new Error('TENANT_REQUIRED'); const { data, error } = await supabase.rpc('import_create_job', { p_company_id: companyId, p_entity_type: input.entity_type ?? 'import', p_total_rows: input.total_rows }); if (error) throw error; return { id: String(data), company_id: companyId, ...input, error_message: null, created_at: new Date().toISOString(), completed_at: null }; }
export async function updateImportRecord(id: string, patch: ImportRecordPatch): Promise<void> { if (!await resolveCurrentCompanyId()) throw new Error('TENANT_REQUIRED'); if (patch.progress !== undefined) { const { error } = await supabase.rpc('import_update_job_progress', { p_job_id: id, p_processed_rows: Math.max(0, Math.round(patch.progress)), p_valid_rows: Math.max(0, Math.round(patch.progress)), p_invalid_rows: 0, p_duplicate_rows: 0, p_status: patch.status ?? 'processing' }); if (error) throw error; } if (patch.status || patch.error_message !== undefined) { const { error } = await supabase.rpc('import_finish_job', { p_job_id: id, p_status: patch.status ?? 'failed', p_result_summary: {}, p_error_message: patch.error_message ?? null }); if (error) throw error; } }
export async function fetchImportRecords(): Promise<ImportRecord[]> { const companyId = await resolveCurrentCompanyId(); if (!companyId) throw new Error('TENANT_REQUIRED'); const { data, error } = await supabase.from('import_jobs').select('id, company_id, job_type, status, total_rows, valid_rows, invalid_rows, progress, error_message, created_at, completed_at, result_summary').eq('company_id', companyId).order('created_at', { ascending: false }); if (error) throw error; return (data ?? []).map(row => ({ id: row.id, company_id: row.company_id, file_name: String((row.result_summary as Record<string, unknown> | null)?.file_name ?? row.job_type ?? 'import'), file_size: 0, source_type: 'import', status: row.status, total_rows: row.total_rows ?? 0, valid_rows: row.valid_rows ?? 0, invalid_rows: row.invalid_rows ?? 0, quarantined_rows: 0, entity_type: row.job_type ?? null, progress: row.progress ?? 0, error_message: row.error_message ?? null, created_at: row.created_at, completed_at: row.completed_at ?? null })); }
export async function markAlertRead(id: string): Promise<void> { const companyId = await resolveCurrentCompanyId(); if (!companyId) throw new Error('TENANT_REQUIRED'); const { error } = await supabase.from('alerts').update({ is_read: true }).eq('id', id).eq('company_id', companyId); if (error) throw error; }
export async function updateRecommendationStatus(id: string, status: string): Promise<void> { const companyId = await resolveCurrentCompanyId(); if (!companyId) throw new Error('TENANT_REQUIRED'); const { error } = await supabase.from('recommendations').update({ status }).eq('id', id).eq('company_id', companyId); if (error) throw error; }
export async function fetchForecasts(): Promise<Forecast[]> { const companyId = await resolveCurrentCompanyId(); if (!companyId) throw new Error('TENANT_REQUIRED'); const { data, error } = await supabase.from('forecasts').select('*').eq('company_id', companyId).order('period', { ascending: true }); if (error) throw error; return (data ?? []) as Forecast[]; }
export async function fetchCustomers(): Promise<Customer[]> { const companyId = await resolveCurrentCompanyId(); if (!companyId) throw new Error('TENANT_REQUIRED'); const { data, error } = await supabase.from('customers').select('*').eq('company_id', companyId).order('name'); if (error) throw error; return (data ?? []) as Customer[]; }
export async function fetchProducts(): Promise<Product[]> { const companyId = await resolveCurrentCompanyId(); if (!companyId) throw new Error('TENANT_REQUIRED'); const { data, error } = await supabase.from('products').select('*').eq('company_id', companyId).order('name'); if (error) throw error; return (data ?? []) as Product[]; }
