export type DocumentRoute='native-text'|'table-extract'|'ocr'|'hybrid';
export interface DocumentProfile{mime:string;hasTextLayer:boolean;hasTables:boolean;isScanned:boolean;languageHint?:string;pages?:number}
export interface DocumentPlan{route:DocumentRoute;steps:string[];reason:string;qualityTarget:number}
export function chooseDocumentRoute(p:DocumentProfile):DocumentPlan{if(p.hasTextLayer&&p.hasTables)return{route:'hybrid',steps:['native text extraction','table extraction','cross-check totals','normalize fields'],reason:'النص والجداول متاحان؛ الدمج يقلل فقدان البيانات',qualityTarget:95};if(p.hasTextLayer)return{route:'native-text',steps:['native extraction','encoding normalization','layout reconstruction','quality checks'],reason:'يوجد نص أصلي قابل للاستخراج',qualityTarget:93};if(p.hasTables)return{route:'table-extract',steps:['table detection','cell extraction','header mapping','numeric validation'],reason:'الملف جدولي ويحتاج استخراج خلايا',qualityTarget:90};return{route:'ocr',steps:['preprocess image','OCR Arabic/English','layout reconstruction','numeric validation','confidence scoring'],reason:'لا توجد طبقة نص؛ OCR هو المسار المناسب',qualityTarget:88};}
