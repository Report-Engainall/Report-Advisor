export interface DecisionEvidence{sourceId:string;period:string;skus:string[];assumptions:string[];confidence:number;freshnessDays:number;sampleSize:number}
export interface EvidenceScore{confidence:number;quality:'high'|'medium'|'low';explanation:string}
export function scoreDecisionEvidence(e:DecisionEvidence):EvidenceScore{const coverage=e.skus.length>0?20:0;const period=e.period.trim()?20:0;const sample=e.sampleSize>=30?25:e.sampleSize>=10?15:e.sampleSize>0?8:0;const freshness=e.freshnessDays<=7?20:e.freshnessDays<=30?12:e.freshnessDays<=90?6:0;const assumptions=e.assumptions.length===0?15:Math.max(0,15-e.assumptions.length*3);const raw=Math.max(0,Math.min(100,Math.round((e.confidence+coverage+period+sample+freshness+assumptions)/2)));return{confidence:raw,quality:raw>=80?'high':raw>=55?'medium':'low',explanation:`evidence=${raw}; source=${e.sourceId}; period=${e.period}; sample=${e.sampleSize}`}}
