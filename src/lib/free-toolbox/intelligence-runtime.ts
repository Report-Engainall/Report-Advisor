import {runAnalytics,type OrchestrationInput} from './analytics-orchestrator';import {planActions,type ActionSignal} from './action-planner';import {evaluateAlerts,type AlertRule,AlertInput} from './alert-engine';import {createReport,type ExecutiveReport,type ReportMetric} from './report-contract';import {buildRenderModel,type RenderModel} from './report-render-model';
export interface IntelligenceRuntimeInput extends OrchestrationInput{signals?:ActionSignal[];alertRules?:AlertRule[];alertInputs?:AlertInput;alertInputsList?:AlertInput[];title?:string;healthScore?:number;period?:string}export interface IntelligenceRuntimeResult{analytics:ReturnType<typeof runAnalytics>;actions:ReturnType<typeof planActions>;alerts:ReturnType<typeof evaluateAlerts>;report:ExecutiveReport;renderModel:RenderModel}
function buildMetrics(analytics:ReturnType<typeof runAnalytics>):ReportMetric[]{const r=analytics.reorder;const metrics:ReportMetric[]=[{key:'inventory.reorderItems',label:'أصناف تحتاج إعادة طلب',value:r.filter(x=>x.status!=='healthy').length,unit:'صنف'},{key:'inventory.coverage',label:'متوسط أيام التغطية',value:r.length?Number((r.reduce((s,x)=>s+(x.stockoutDays??0),0)/r.length).toFixed(1)):null,unit:'يوم'},{key:'inventory.recommendedOrder',label:'الكمية المقترحة للطلب',value:r.reduce((s,x)=>s+x.recommendedOrder,0),unit:'وحدة'},{key:'data.quality',label:'جودة البيانات',value:analytics.quality,unit:'%'}];if(analytics.cash){metrics.push({key:'cash.projected7d',label:'صافي التدفق المتوقع 7 أيام',value:analytics.cash.projected7d,unit:'عملة'});metrics.push({key:'cash.projected30d',label:'صافي التدفق المتوقع 30 يومًا',value:analytics.cash.projected30d,unit:'عملة'});}if(analytics.scenario)metrics.push({key:'cash.scenarioRisk',label:'مخاطر السيناريو',value:analytics.scenario.score,unit:'score'});return metrics}
export function runIntelligenceRuntime(input:IntelligenceRuntimeInput):IntelligenceRuntimeResult{const analytics=runAnalytics(input);const actions=planActions(input.signals??[],Math.max(50,analytics.quality-25));const alerts=evaluateAlerts(input.alertRules??[],input.alertInputsList??(input.alertInputs?[input.alertInputs]:[]));const metrics=buildMetrics(analytics);const report=createReport(input.title??'التقرير الذكي التنفيذي',input.healthScore??analytics.quality,metrics,actions.map(a=>({id:`action-${a.title}`,title:a.title,reason:a.reason,priority:a.priority,impact:a.impact,confidence:a.confidence,evidenceIds:a.evidenceIds})),[...analytics.warnings,...alerts.map(a=>a.message)],analytics.quality,input.period);return{analytics,actions,alerts,report,renderModel:buildRenderModel(report,actions,alerts)}}
