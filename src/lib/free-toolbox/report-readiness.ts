export interface ReadinessInput{availableDomains:string[];dataQuality:number;recordCount:number;hasEvidence:boolean;criticalAlerts:number}
export interface ReadinessResult{ready:boolean;score:number;status:'ready'|'review'|'blocked';reasons:string[]}
export function evaluateReportReadiness(x:ReadinessInput):ReadinessResult{const reasons:string[]=[];let score=0;score+=Math.min(25,x.availableDomains.length*4);score+=Math.min(30,Math.max(0,x.dataQuality)*.3);score+=x.recordCount>0?20:0;score+=x.hasEvidence?15:0;score+=x.criticalAlerts===0?10:0;if(!x.recordCount)reasons.push('لا توجد سجلات قابلة للتحليل');if(x.dataQuality<60)reasons.push('جودة البيانات منخفضة');if(!x.hasEvidence)reasons.push('لا توجد أدلة مصدرية كافية');if(x.criticalAlerts>0)reasons.push(`يوجد ${x.criticalAlerts} تنبيه حرج يحتاج مراجعة`);score=Math.round(Math.min(100,score));return{ready:score>=80&&x.criticalAlerts===0,score,status:score<60?'blocked':score<80?'review':'ready',reasons};}
