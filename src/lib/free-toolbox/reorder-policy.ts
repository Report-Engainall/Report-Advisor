export interface ReorderInput{stock:number;dailyDemand:number;leadTimeDays:number;serviceLevelZ?:number;safetyDays?:number;maxDays?:number;}
export interface ReorderResult{reorderPoint:number;safetyStock:number;minimumStock:number;maximumStock:number;recommendedOrder:number;stockoutDays:number|null;status:'healthy'|'reorder'|'urgent';}
export function calculateReorderPolicy(x:ReorderInput):ReorderResult{const z=x.serviceLevelZ??1.65;const safety=Math.max(0,x.dailyDemand*(x.safetyDays??3)*z);const minimum=Math.max(0,x.dailyDemand*x.leadTimeDays+safety);const maximum=Math.max(minimum,x.dailyDemand*(x.maxDays??30)+safety);const order=Math.max(0,maximum-x.stock);const stockout=x.dailyDemand>0?x.stock/x.dailyDemand:null;const status=stockout!==null&&stockout<=3?'urgent':x.stock<=minimum?'reorder':'healthy';return{reorderPoint:minimum,safetyStock:safety,minimumStock:minimum,maximumStock:maximum,recommendedOrder:order,stockoutDays:stockout,status};}
