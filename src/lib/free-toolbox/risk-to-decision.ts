import {arbitrateDecisions,type DecisionCandidate,type ArbitratedDecision} from './decision-arbitrator';
import type {RiskAlert} from './risk-early-warning';
export interface RiskDecisionBundle{alerts:RiskAlert[];decisions:ArbitratedDecision[];criticalCount:number;recommendedFocus:string[];}
export function riskAlertsToDecisions(alerts:RiskAlert[]):RiskDecisionBundle{const candidates:DecisionCandidate[]=alerts.map(a=>({id:a.id,title:a.label,category:a.domain==='inventory'?'inventory':a.domain==='liquidity'?'cash':a.domain==='receivable'?'receivable':a.domain==='payable'?'payable':'sales',amount:0,urgency:a.score,impact:a.score,confidence:Math.min(100,60+a.signals.length*10),liquidityEffect:a.domain==='liquidity'?-a.score*.5:a.domain==='receivable'?a.score*.4:a.domain==='payable'?-a.score*.2:0,evidenceCount:a.signals.length,reversible:true}));const decisions=arbitrateDecisions(candidates).decisions;return{alerts,decisions,criticalCount:decisions.filter(d=>d.band==='critical').length,recommendedFocus:decisions.slice(0,5).map(d=>d.title)};}
