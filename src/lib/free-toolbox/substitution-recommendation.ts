import {AlternativeGroupAggregate} from './alternative-groups';
export interface SubstitutionRecommendation{sku:string;groupId?:string;action:'reorder_sku'|'reorder_group'|'do_not_reorder';reason:string;priority:number;groupCoverageDays?:number;}
export function recommendReplenishment(sku:string,skuDaysOfStock:number,group?:AlternativeGroupAggregate,leadTimeDays=7,safetyDays=2):SubstitutionRecommendation{const threshold=leadTimeDays+safetyDays;if(!group)return{sku,action:skuDaysOfStock<threshold?'reorder_sku':'do_not_reorder',priority:skuDaysOfStock<=0?100:skuDaysOfStock<threshold?80:10,reason:skuDaysOfStock<threshold?'الصنف يحتاج إعادة تزويد بناءً على تغطيته الحالية':'التغطية الحالية كافية'};if(group.daysOfCover>=threshold)return{sku,groupId:group.groupId,action:'do_not_reorder',priority:15,groupCoverageDays:group.daysOfCover,reason:'التغطية المجمعة للمجموعة كافية؛ لا تُنشأ عملية شراء للصنف منفردًا'};return{sku,groupId:group.groupId,action:'reorder_group',priority:group.daysOfCover<=0?100:90,groupCoverageDays:group.daysOfCover,reason:'التغطية المجمعة للمجموعة أقل من مدة التوريد والأمان؛ القرار يجب أن يغطي البدائل المتاحة'}}
