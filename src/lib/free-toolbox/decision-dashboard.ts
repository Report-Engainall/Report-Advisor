export type DecisionCardKind='stockout'|'demand'|'lost_sales'|'continuity'|'alternative'|'liquidity'|'replenishment'
export interface DecisionCard{kind:DecisionCardKind;title:string;value:number;unit:string;priority:number;confidence:number;target?:string;reason:string}
export interface DashboardSignals{stockoutCount:number;acceleratingCount:number;lostUnits:number;fillRate:number;alternativeRiskCount:number;liquidityScore:number;replenishmentCount:number;confidence:number}
export function buildDecisionCards(s:DashboardSignals):DecisionCard[]{const confidence=Math.max(0,Math.min(100,s.confidence));return[{kind:'stockout',title:'Stockout Risk',value:s.stockoutCount,unit:'items',priority:s.stockoutCount?95:10,confidence,target:'inventory',reason:'items below safe coverage'},{kind:'demand',title:'Demand Pulse',value:s.acceleratingCount,unit:'items',priority:s.acceleratingCount?80:10,confidence,target:'demand',reason:'items with accelerating demand'},{kind:'lost_sales',title:'Lost Sales',value:s.lostUnits,unit:'units',priority:s.lostUnits?90:10,confidence,target:'customers',reason:'requested units not fulfilled'},{kind:'continuity',title:'Customer Continuity',value:Math.round(Math.max(0,Math.min(1,s.fillRate))*100),unit:'%',priority:s.fillRate<.8?88:20,confidence,target:'fill-rate',reason:'customer demand fulfillment'},{kind:'alternative',title:'Alternative Coverage',value:s.alternativeRiskCount,unit:'groups',priority:s.alternativeRiskCount?82:15,confidence,target:'groups',reason:'alternative groups lacking adequate coverage'},{kind:'liquidity',title:'Liquidity Leaders',value:Math.round(Math.max(0,Math.min(1,s.liquidityScore))*100),unit:'score',priority:60,confidence,target:'cash-flow',reason:'relative operating liquidity contribution'},{kind:'replenishment',title:'Replenishment Queue',value:s.replenishmentCount,unit:'actions',priority:s.replenishmentCount?92:10,confidence,target:'purchasing',reason:'recommended replenishment actions'}].sort((a,b)=>b.priority-a.priority)}
