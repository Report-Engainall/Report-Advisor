import { cashConversionCycle, projectLiquidity, type CashConversionCycle, type LiquidityProjection } from '../businessIntelligenceEngines';
import { prioritizeReceivables, prioritizeSupplierPayments, protectCashReserve, type PaymentPriority, type ReceivablePriority, type ReserveProtection } from '../financialDecisionEngines';
export interface FinancialIntelligenceInput { revenue:number; receivables:number; inventory:number; costOfSales:number|null; payables:number; purchases:number; openingCash:number; dailyInflow:number; dailyOutflow:number; committedOutflow?:number; receivablesItems?:Parameters<typeof prioritizeReceivables>[0]; supplierItems?:Parameters<typeof prioritizeSupplierPayments>[0]; periodDays?:number; }
export interface FinancialIntelligence { ccc:CashConversionCycle; liquidity:LiquidityProjection[]; reserve:ReserveProtection; collections:ReceivablePriority[]; supplierPayments:PaymentPriority[]; profitability:{revenue:number;costOfSales:number|null;grossProfit:number|null;marginPct:number|null;status:'READY'|'PROFIT_UNAVAILABLE'}; warnings:string[]; }
export function buildFinancialIntelligence(input:FinancialIntelligenceInput):FinancialIntelligence { const revenue=Math.max(0,input.revenue); const cost=input.costOfSales!=null&&Number.isFinite(input.costOfSales)&&input.costOfSales>=0?input.costOfSales:null; const grossProfit=cost==null?null:revenue-cost; const marginPct=cost==null||revenue<=0?null:grossProfit!/revenue*100; const ccc=cashConversionCycle({receivables:Math.max(0,input.receivables),revenue,inventory:Math.max(0,input.inventory),costOfSales:cost,payables:Math.max(0,input.payables),purchases:Math.max(0,input.purchases),periodDays:input.periodDays??365}); const liquidity=projectLiquidity({openingLiquidity:Math.max(0,input.openingCash),horizons:[0,7,15,30,60,90],dailyInflow:Math.max(0,input.dailyInflow),dailyOutflow:Math.max(0,input.dailyOutflow),committedOutflow:Math.max(0,input.committedOutflow??0)}); const reserve=protectCashReserve({openingCash:Math.max(0,input.openingCash),committedOutflow:Math.max(0,input.committedOutflow??0),collectibleInflow:Math.max(0,input.dailyInflow)*30}); const collections=prioritizeReceivables(input.receivablesItems??[]); const supplierPayments=prioritizeSupplierPayments(input.supplierItems??[],reserve); const warnings:string[]=[]; if(cost==null)warnings.push('PROFIT_UNAVAILABLE: تكلفة المبيعات الفعلية غير متاحة؛ لم يتم استخدام المشتريات كبديل.'); if(ccc.status!=='READY')warnings.push('CCC غير مكتمل بسبب نقص بيانات التكلفة/المبيعات/المشتريات.'); if(liquidity.some(row=>row.status==='GAP'))warnings.push('يوجد عجز سيولة متوقع ضمن أحد الآفاق الزمنية.'); if(reserve.blockedAmount>0)warnings.push('حماية الاحتياطي النقدي مفعلة؛ يجب عدم استنزاف النقد التشغيلي.'); return{ccc,liquidity,reserve,collections,supplierPayments,profitability:{revenue,costOfSales:cost,grossProfit,marginPct,status:cost==null?'PROFIT_UNAVAILABLE':'READY'},warnings}; }
