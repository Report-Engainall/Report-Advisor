import {describe,expect,it} from 'vitest'; import {suggestProductFamilies} from './product-family-discovery';
describe('product-family-discovery',()=>{it('groups compatible category size and unit candidates',()=>{const r=suggestProductFamilies([{id:'a',name:'زيت أ',category:'زيت',unit:'دبة',sizeValue:20,sizeUnit:'لتر',brand:'A'},{id:'b',name:'زيت ب',category:'زيت',unit:'دبة',sizeValue:20,sizeUnit:'لتر',brand:'B'},{id:'c',name:'زيت ج',category:'زيت',unit:'دبة',sizeValue:5,sizeUnit:'لتر',brand:'A'}]);expect(r).toHaveLength(1);expect(r[0].productIds).toEqual(['a','b']);expect(r[0].needsReview).toBe(true);});it('does not group without a stable key',()=>{expect(suggestProductFamilies([{id:'a',name:'x'},{id:'b',name:'y'}])).toHaveLength(0);});});
