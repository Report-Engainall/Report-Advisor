import{describe,expect,it}from'vitest';import{acknowledgeAlert,createDecisionEvidence,resolveAlert,snoozeAlert,upsertAlert}from'./decision-evidence-ledger';
describe('decision-evidence-ledger',()=>{it('stores auditable evidence',()=>{const r=createDecisionEvidence({decisionId:'d1',productId:'p1',generation:3,action:'buy_now',confidence:.9,evidence:[{metric:'stock',value:10,source:'inventory'}],warnings:[],inputFingerprint:'abc'});expect(r.evidence[0].source).toBe('inventory');});it('deduplicates repeated alerts and reopens resolved alerts',()=>{let a=upsertAlert(undefined,{dedupeKey:'p1:stock',productId:'p1',type:'stockout'});a=acknowledgeAlert(a);a=upsertAlert(a,{dedupeKey:'p1:stock',productId:'p1',type:'stockout'});expect(a.occurrences).toBe(2);a=resolveAlert(a);a=upsertAlert(a,{dedupeKey:'p1:stock',productId:'p1',type:'stockout'});expect(a.status).toBe('reopened');});it('supports snooze lifecycle',()=>{let a=upsertAlert(undefined,{dedupeKey:'x',productId:'p',type:'d'});a=snoozeAlert(a);expect(a.status).toBe('snoozed');});});