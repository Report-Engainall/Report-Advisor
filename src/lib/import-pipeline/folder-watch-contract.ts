export interface WatchedFolderConfig { id:string; path:string; enabled:boolean; recursive:boolean; pollIntervalMs:number; allowedExtensions:string[]; autoProcess:boolean; maxConcurrent:number; }
export interface WatchEvent { folderId:string; path:string; event:'created'|'changed'|'deleted'|'scan'; observedAt:string; }
export interface ImportQueueItem { id:string; folderId:string; path:string; discoveredAt:string; attempts:number; status:'queued'|'processing'|'completed'|'failed'|'dead_letter'; lastError?:string; }
export function normalizeWatchConfig(input:Partial<WatchedFolderConfig>):WatchedFolderConfig { return {id:String(input.id??''),path:String(input.path??'').trim(),enabled:input.enabled!==false,recursive:input.recursive!==false,pollIntervalMs:Math.max(1000,Number(input.pollIntervalMs??5000)),allowedExtensions:(input.allowedExtensions??['.pdf','.xlsx','.xls','.csv','.docx','.doc','.txt']).map(x=>String(x).toLowerCase().startsWith('.')?String(x).toLowerCase():`.${String(x).toLowerCase()}`),autoProcess:input.autoProcess!==false,maxConcurrent:Math.max(1,Math.min(8,Number(input.maxConcurrent??2)))}; }
export function shouldQueue(event:WatchEvent, config:WatchedFolderConfig):boolean { return config.enabled && (event.event==='created'||event.event==='changed'||event.event==='scan') && config.allowedExtensions.some(ext=>event.path.toLowerCase().endsWith(ext)); }
