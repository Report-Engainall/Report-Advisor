import {describe,expect,it} from 'vitest'; import {catalogHeadersFor} from './accounting-system-header-catalog';
describe('accounting-system-header-catalog',()=>{it('exposes inventory headers for all supported Arabic ERP sources',()=>{for(const system of ['onyx-pro','motakamel-plus','rogsh','generic-arabic-erp'] as const){const h=catalogHeadersFor(system,'inventory');expect(h.some(x=>x.field==='sku')).toBe(true);expect(h.some(x=>x.field==='current_stock')).toBe(true);}});it('includes Arabic and English aliases',()=>{const h=catalogHeadersFor('onyx-pro','sales');const sku=h.find(x=>x.field==='sku')!;expect(sku.aliases).toContain('رقم الصنف');expect(sku.aliases).toContain('SKU');});});
