import {describe,expect,it} from 'vitest'; import {findOnyxHeaders,getOnyxProHeaderCatalog} from './onyx-pro-header-catalog';
describe('onyx-pro-header-catalog',()=>{it('contains broad report coverage',()=>{const c=getOnyxProHeaderCatalog();expect(c.length).toBeGreaterThan(70);expect(c.some(x=>x.canonical==='sku')).toBe(true);expect(c.some(x=>x.canonical==='unfulfilled')).toBe(false);});it('matches Arabic and English aliases',()=>{const r=findOnyxHeaders(['رقم الصنف','اسم الصنف','الرصيد الحالي','تاريخ الحركة','Gross Profit','نسبة الربحية','رقم العميل']);expect(r.map(x=>x.canonical)).toEqual(expect.arrayContaining(['sku','item_name','current_balance','date','gross_profit','profit_margin','customer_id']));});});
