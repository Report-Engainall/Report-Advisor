import {describe,expect,it} from 'vitest'; import {advanceFile,canResume} from './file-lifecycle-engine';
describe('file-lifecycle-engine',()=>{it('advances one stage at a time',()=>{let s={fileId:'f1',stage:'detected' as const,attempt:0,updatedAt:'x'};s=advanceFile(s,'validated','t');expect(s.stage).toBe('validated');expect(()=>advanceFile(s,'imported','t')).toThrow();});it('records failures and supports retry',()=>{const s=advanceFile({fileId:'f1',stage:'parsed',attempt:1,updatedAt:'x'},'failed','t','parse error');expect(s.stage).toBe('failed');expect(s.attempt).toBe(2);expect(canResume(s)).toBe(true);});it('does not resume completed files',()=>{expect(canResume({fileId:'f1',stage:'archived',attempt:1,updatedAt:'x'})).toBe(false);});});
