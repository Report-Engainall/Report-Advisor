export interface ReportSnapshot {snapshotId:string;sourceKey:string;contentHash:string;version:number;observedAt:string;rowCount:number;newRows:number;updatedRows:number;unchangedRows:number;deletedRows:number;}
export interface ImpactedAnalysisPlan {snapshotId:string;affectedKeys:string[];recompute:string[];reason:string;}
export function createSnapshot(input:Omit<ReportSnapshot,'version'> & {previous?:ReportSnapshot}):ReportSnapshot{return {...input,version:(input.previous?.version??0)+1};}
export function planImpactedAnalysis(snapshot:ReportSnapshot,changedKeys:string[]):ImpactedAnalysisPlan {const recompute=['report-quality','inventory-movement','demand-history'];if(snapshot.newRows+snapshot.updatedRows+snapshot.deletedRows>0)recompute.push('customer-product-history','forecast-features','replenishment-risk');return {snapshotId:snapshot.snapshotId,affectedKeys:[...new Set(changedKeys)],recompute:[...new Set(recompute)],reason:`snapshot v${snapshot.version} contains ${snapshot.newRows+snapshot.updatedRows+snapshot.deletedRows} changed/deleted rows`};}
