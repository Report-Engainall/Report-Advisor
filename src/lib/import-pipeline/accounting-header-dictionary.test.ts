import {describe,expect,it} from 'vitest'; import {canonicalizeHeader} from './accounting-header-dictionary';
describe('accounting-header-dictionary',()=>{it('maps Arabic accounting headers',()=>{expect(canonicalizeHeader('رقم الصنف')).toBe('sku');expect(canonicalizeHeader('سعر البيع')).toBe('price');expect(canonicalizeHeader('رقم العميل')).toBe('customer_id');expect(canonicalizeHeader('المخزن')).toBe('warehouse');});it('maps common English variants',()=>{expect(canonicalizeHeader('Item Code')).toBe('sku');expect(canonicalizeHeader('Unit Price')).toBe('price');expect(canonicalizeHeader('Invoice Number')).toBe('invoice');});});
