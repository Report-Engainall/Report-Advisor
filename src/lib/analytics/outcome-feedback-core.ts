export type OutcomeLabel = 'correct' | 'incorrect' | 'partial' | 'unknown';
export interface DecisionOutcome { tenantId: string; decisionFingerprint: string; evidenceSnapshotId: string; actionId?: string; observedAt: string; label: OutcomeLabel; actualValue?: number; expectedValue?: number; impactValue?: number; notes?: string; }
export interface OutcomeQuality { count: number; accuracy: number | null; coverage: number | null; impact: number | null; }

function finiteOptional(value: number | undefined, field: string): number | undefined { if (value === undefined) return undefined; if (!Number.isFinite(value)) throw new Error(`OUTCOME_INVALID_NUMBER:${field}`); return value; }
function validateOutcome(outcome: DecisionOutcome): void { if (!outcome.tenantId || !outcome.decisionFingerprint || !outcome.evidenceSnapshotId) throw new Error('Outcome identity is incomplete'); const observed = new Date(outcome.observedAt).getTime(); if (!Number.isFinite(observed)) throw new Error('Outcome timestamp is invalid'); finiteOptional(outcome.actualValue, 'actualValue'); finiteOptional(outcome.expectedValue, 'expectedValue'); finiteOptional(outcome.impactValue, 'impactValue'); if (outcome.label !== 'unknown' && (outcome.actualValue === undefined || outcome.expectedValue === undefined)) throw new Error('OUTCOME_VALUES_REQUIRED_FOR_KNOWN_LABEL'); }
export function recordOutcome(outcomes: DecisionOutcome[], outcome: DecisionOutcome): DecisionOutcome[] { validateOutcome(outcome); const duplicate = outcomes.some(item => item.tenantId === outcome.tenantId && item.decisionFingerprint === outcome.decisionFingerprint); return duplicate ? outcomes : [...outcomes, { ...outcome }]; }
export function summarizeOutcomes(outcomes: DecisionOutcome[], tenantId: string): OutcomeQuality { const scoped = outcomes.filter(item => item.tenantId === tenantId); if (!scoped.length) return { count: 0, accuracy: null, coverage: null, impact: null }; const known = scoped.filter(item => item.label !== 'unknown'); const correct = known.filter(item => item.label === 'correct').length; const partial = known.filter(item => item.label === 'partial').length; const impacts = scoped.map(item => item.impactValue).filter((value): value is number => Number.isFinite(value)); return { count: scoped.length, accuracy: known.length ? (correct + partial * 0.5) / known.length : null, coverage: scoped.length ? known.length / scoped.length : null, impact: impacts.length ? impacts.reduce((sum, value) => sum + value, 0) : null }; }
