export type FinancialSeverity='info'|'warning'|'critical';
export interface FinancialEvidence{code:string;severity:FinancialSeverity;message:string;source?:string;field?:string;expected?:number;actual?:number;difference?:number;}
export interface FinancialAnomalyInput{source:string;reported:number;expected:number;currency?:string;reference?:string;}
export function buildFinancialEvidence(i:FinancialAnomalyInput):FinancialEvidence[]{const d=i.reported-i.expected;const tolerance=Math.max(.01,Math.abs(i.expected)*1e-9);if(Math.abs(d)<=tolerance)return[{code:'BALANCED',severity:'info',message:'القيمة الواردة متوافقة مع القيمة المحسوبة',source:i.source,expected:i.expected,actual:i.reported,difference:d}];const ratio=Math.abs(i.expected)>0?Math.abs(d/i.expected):1;const severity:FinancialSeverity=ratio>=.1?'critical':ratio>=.01?'warning':'info';return[{code:'FINANCIAL_MISMATCH',severity,message:'يوجد فرق بين القيمة الواردة والقيمة المحسوبة',source:i.source,field:i.reference,expected:i.expected,actual:i.reported,difference:d}];}
