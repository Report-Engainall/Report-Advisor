import{describe,expect,it}from'vitest';import{mapHeader,mapHeaders}from'./semantic-field-mapper';
describe('semantic-field-mapper',()=>{it('uses exact aliases without review',()=>{const r=mapHeader('رقم الصنف');expect(r.field).toBe('productCode');expect(r.confidence).toBe(1);expect(r.requiresReview).toBe(false);});it('supports manual overrides',()=>{const r=mapHeader('رقم المادة','productCode');expect(r.method).toBe('manual');expect(r.confidence).toBe(1);});it('flags uncertain fuzzy mappings',()=>{const r=mapHeader('اسم الصنف التجاري');expect(r.requiresReview).toBe(true);});it('maps batches',()=>{const r=mapHeaders(['رقم الصنف','اسم الصنف','الرصيد الحالي']);expect(r.every(x=>x.field)).toBe(true);});});