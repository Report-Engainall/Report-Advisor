import{describe,expect,it}from'vitest';import{reconcileRows}from'./reconciliation-engine';
const prev=[{rowId:'1',fingerprint:'a',productCode:'P1',quantity:10,active:true},{rowId:'2',fingerprint:'b',productCode:'P2',quantity:5,active:true}];
describe('reconciliation-engine',()=>{it('detects new and unchanged rows',()=>{const r=reconcileRows(prev,[prev[0],{rowId:'3',fingerprint:'c',productCode:'P3'}]);expect(r.unchangedCount).toBe(1);expect(r.newCount).toBe(1);});it('detects changes and reversals',()=>{const r=reconcileRows(prev,[{...prev[0],fingerprint:'z',quantity:20},{...prev[1],active:false}]);expect(r.changedCount).toBe(1);expect(r.reversedCount).toBe(1);});it('detects duplicate rows in incoming data',()=>{const r=reconcileRows([], [{rowId:'1',fingerprint:'a'},{rowId:'1',fingerprint:'a'}]);expect(r.duplicateCount).toBe(1);});it('flags identity conflicts',()=>{const r=reconcileRows(prev,[{...prev[0],fingerprint:'z',productCode:'OTHER'}]);expect(r.conflictCount).toBe(1);});});