export interface LedgerAmounts{debit?:number;credit?:number;balance?:number;openingDebit?:number;openingCredit?:number;closingDebit?:number;closingCredit?:number;}
export interface FinancialReconciliation{openingBalance:number;movementNet:number;expectedBalance:number;reportedBalance:number|null;difference:number|null;status:'balanced'|'mismatch'|'insufficient_data';reasons:string[];}
export function reconcileFinancialStatement(a:LedgerAmounts):FinancialReconciliation{const opening=(a.openingCredit??0)-(a.openingDebit??0);const movement=(a.credit??0)-(a.debit??0);const expected=opening+movement;const reported=a.balance??(a.closingCredit!=null||a.closingDebit!=null?(a.closingCredit??0)-(a.closingDebit??0):null);if(reported===null)return{openingBalance:opening,movementNet:movement,expectedBalance:expected,reportedBalance:null,difference:null,status:'insufficient_data',reasons:['لا يوجد رصيد نهائي قابل للمقارنة']};const difference=reported-expected;const tolerance=Math.max(.01,Math.abs(expected)*1e-9);const balanced=Math.abs(difference)<=tolerance;return{openingBalance:opening,movementNet:movement,expectedBalance:expected,reportedBalance:reported,difference,status:balanced?'balanced':'mismatch',reasons:balanced?['الرصيد متوافق حسابيًا']:['يوجد فرق بين الرصيد المتوقع والرصيد الوارد في الكشف']};}
