import{describe,expect,it}from'vitest';import{planIngestion}from'./universal-ingestion-planner';
describe('universal-ingestion-planner',()=>{it('auto processes a strong inventory signature',()=>{const p=planIngestion(['رقم الصنف','الرصيد الحالي']);expect(p.reportType).toBe('inventory');expect(p.action).toBe('auto_process');});it('requires review for unknown structures',()=>{const p=planIngestion(['عمود غريب']);expect(p.action).toBe('review_mapping');});it('does not guess a required field',()=>{const p=planIngestion(['اسم الصنف','الكمية']);expect(p.action).toBe('review_mapping');expect(p.requiredMissing.length).toBeGreaterThan(0);});});