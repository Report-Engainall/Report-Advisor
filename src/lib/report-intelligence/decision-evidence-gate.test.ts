import{describe,expect,it}from'vitest';import{evaluateDecisionEvidenceGate}from'./decision-evidence-gate';
const base={productId:'P1',productName:'Test',action:'buy_now' as const,priority:80,confidence:.8,recommendedQty:10,capitalRequired:100,coverageDays:5,evidence:[{metric:'reorderGap',value:10,meaning:'gap'}],warnings:[],reason:'urgent'};
describe('decision-evidence-gate',()=>{it('allows a sufficiently evidenced decision',()=>expect(evaluateDecisionEvidenceGate({decision:base}).allowed).toBe(true));it('blocks low-confidence execution',()=>expect(evaluateDecisionEvidenceGate({decision:{...base,confidence:.4}}).allowed).toBe(false));it('blocks unlinked high-risk anomalies',()=>{const r=evaluateDecisionEvidenceGate({decision:base,anomalies:[{kind:'balance_mismatch',severity:'high',score:90,message:'mismatch',evidence:['balance']} ]});expect(r.allowed).toBe(false);expect(r.blockingReasons.length).toBeGreaterThan(0);});it('allows high-risk anomalies when evidence is linked',()=>{const r=evaluateDecisionEvidenceGate({decision:base,anomalies:[{kind:'balance_mismatch',severity:'high',score:90,message:'mismatch',evidence:['balance']}],links:[{anomalyKind:'balance_mismatch',source:'statement.xlsx',field:'balance',reference:'row-10',value:'100',confidence:.95}]});expect(r.allowed).toBe(true);});});