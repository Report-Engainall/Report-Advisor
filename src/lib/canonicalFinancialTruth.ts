export type FinancialTruthStatus = 'CALCULABLE' | 'INSUFFICIENT_DATA' | 'UNKNOWN' | 'EXCLUDED';
export interface GrossProfitInput { line_total:number|null|undefined; cost_price:number|null|undefined; quantity:number|null|undefined; }
export interface GrossProfitResult { status:FinancialTruthStatus; revenue:number|null; costOfSales:number|null; grossProfit:number|null; }
const finite=(v:number|null|undefined):v is number=>v!==null&&v!==undefined&&Number.isFinite(v);
export function calculateGrossProfit(rows:GrossProfitInput[]):GrossProfitResult{if(!rows.length)return{status:'INSUFFICIENT_DATA',revenue:null,costOfSales:null,grossProfit:null};let revenue=0,costOfSales=0;for(const row of rows){if(!finite(row.line_total)||!finite(row.cost_price)||!finite(row.quantity))return{status:'INSUFFICIENT_DATA',revenue:null,costOfSales:null,grossProfit:null};revenue+=row.line_total;costOfSales+=row.cost_price*row.quantity;}return{status:'CALCULABLE',revenue,costOfSales,grossProfit:revenue-costOfSales};}
export function assertGrossProfitInvariant(result:GrossProfitResult):void{if(result.status!=='CALCULABLE'){if(result.revenue!==null||result.costOfSales!==null||result.grossProfit!==null)throw new Error('FINANCIAL_TRUTH_STATUS_VALUE_MISMATCH');return;}if(result.revenue===null||result.costOfSales===null||result.grossProfit===null)throw new Error('FINANCIAL_TRUTH_CALCULABLE_VALUE_MISSING');if(result.grossProfit!==result.revenue-result.costOfSales)throw new Error('FINANCIAL_TRUTH_GROSS_PROFIT_FORMULA_MISMATCH');}
