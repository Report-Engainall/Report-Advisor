/**
 * Legacy import surface only.
 *
 * Business truth is owned exclusively by ./queries. Keep this file as a thin
 * re-export until external consumers have been migrated; do not add logic here.
 */
export * from './queries';
